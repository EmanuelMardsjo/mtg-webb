---
phase: 04-resident-stories-block
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/system/examples/ResidentStories/types.ts
  - src/system/examples/ResidentStories/ResidentStories.tsx
autonomous: true
requirements: [REQ-01, REQ-03, REQ-04, REQ-06]

must_haves:
  truths:
    - "A reader sees one resident story at a time as the page scrolls (active story promoted, others recede)"
    - "Each story is a real article with an eyebrow, an h3 headline, and an attribution"
    - "A story with an href renders as a navigating link; a story without href is a non-interactive article (no dead click target)"
    - "Without scroll JS (no IntersectionObserver), the first story is active and all story text is in the DOM (SC#2 baseline)"
    - "The block derives its active story from useScrollSections — no manual scroll listener, no per-frame DOM writes"
  artifacts:
    - path: "src/system/examples/ResidentStories/types.ts"
      provides: "ResidentStoriesContent + ResidentStory content shape (optional eyebrow/heading, stories[], optional theme)"
      contains: "export type ResidentStoriesContent"
    - path: "src/system/examples/ResidentStories/ResidentStories.tsx"
      provides: "ResidentStories component: Section(pink-soft) > block > sticky stage (progress + cross-faded story panels) + aria-hidden track of registered anchors"
      exports: ["ResidentStories", "ResidentStoriesProps"]
      min_lines: 50
  key_links:
    - from: "src/system/examples/ResidentStories/ResidentStories.tsx"
      to: "@system/hooks useScrollSections"
      via: "useScrollSections<string>(ids) wired to register() on anchor divs; activeIndex = ids.indexOf(activeId)"
      pattern: "useScrollSections"
    - from: "src/system/examples/ResidentStories/ResidentStories.tsx"
      to: "progress fill"
      via: "inline --progress = (activeIndex + 1) / stories.length CSS var on the progress element"
      pattern: "--progress"
---

<objective>
Create the content type and the `ResidentStories` React component — the sticky scroll-driven resident-story sequence. The component composes a `Section theme="pink-soft"`, a block-local sticky **stage** (containing the decorative progress indicator and all story panels, cross-faded by an `is-active` class), and a tall `aria-hidden` **track** of one full-viewport anchor per story registered via `useScrollSections`. It holds NO manual scroll machinery — `useScrollSections` (the proven `MTGNeighborhoods` reuse, D-2) derives the active story index, which drives both the `is-active` class and the inline `--progress` CSS var.

Purpose: Establishes the block anatomy (REQ-01, REQ-03), the scroll-hook reuse (REQ-04, SC#3), and the no-JS-readable structure (SC#2). The CSS that turns this structure into a sticky cross-fading sequence — plus the story, test, and barrel — lands in 04-02.

Output: `types.ts` and `ResidentStories.tsx`. (Until 04-02 adds the CSS module, story, and barrel, this plan's `npm run typecheck` will fail on the missing `./ResidentStories.module.css` import — see Task 2 note; lint/typecheck of the full block is gated in 04-02.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-resident-stories-block/04-UI-SPEC.md
@.planning/phases/04-resident-stories-block/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResidentStories content types</name>
  <files>src/system/examples/ResidentStories/types.ts</files>
  <read_first>
- src/system/examples/CrashCourseRail/types.ts (the exact analog: ThemeName import, per-item shape with optional href, Content with theme?)
- .planning/phases/04-resident-stories-block/04-UI-SPEC.md lines 72-93 (the ResidentStoriesContent shape, D-7 href, D-8 optional eyebrow/heading)
- .planning/codebase/CONVENTIONS.md §Types (name CMS content types {ComponentName}Content; literal unions)
  </read_first>
  <action>
Create the folder `src/system/examples/ResidentStories/` and `types.ts` inside it. Mirror the CrashCourseRail/types.ts structure. Import `type { ThemeName } from '@tokens/themes'`. Export `type ResidentStory` with fields: `eyebrow: string` (origin → destination), `headline: string` (the pulled quote; supports `\n`), `attribution: string`, `attributionName?: string` (optional bold lead, per D-9 — kept body-400, no 700), `image: { src: string; alt: string }`, `side?: 'left' | 'right'` (optional media side; default alternates by index), `href?: string` (D-7 — with href the story is a navigating `<a>`, without it a non-interactive `<article>`). Export `type ResidentStoriesContent` with: `eyebrow?: string` (OPTIONAL intro eyebrow, D-8 — source has none), `heading?: string` (OPTIONAL intro heading, D-8 — the Section aria-label carries the title by default), `stories: ResidentStory[]`, `theme?: ThemeName` (default `'pink-soft'` is applied in the component, not here). Match house style: 2-space indent, single quotes, no trailing semicolons. No `eslint-disable`, no `any`.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -v "ResidentStories.module.css" | grep -i "types.ts" ; test $? -ne 0 && echo "types.ts has no own type errors"</automated>
  </verify>
  <acceptance_criteria>
- `src/system/examples/ResidentStories/types.ts` exists and exports `ResidentStoriesContent` and `ResidentStory`.
- `grep -c "export type ResidentStoriesContent" src/system/examples/ResidentStories/types.ts` returns 1; same for `export type ResidentStory`.
- Both `eyebrow` and `heading` on `ResidentStoriesContent` are optional (`grep -E "eyebrow\?:|heading\?:" types.ts` matches both) — D-8.
- `href?: string` present on `ResidentStory` (D-7); `side?: 'left' | 'right'` present.
- `import type { ThemeName } from '@tokens/themes'` present; `theme?: ThemeName` on `ResidentStoriesContent`.
- No `any`, no `eslint-disable` in the file (`grep -E "\bany\b|eslint-disable" types.ts` returns nothing).
  </acceptance_criteria>
  <done>types.ts defines the CMS content shape with optional intro eyebrow/heading (D-8), optional per-story href (D-7) and side, and a theme override — matching the CrashCourseRail types idiom.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ResidentStories component (sticky stage + track + progress, useScrollSections wiring)</name>
  <files>src/system/examples/ResidentStories/ResidentStories.tsx</files>
  <read_first>
- src/system/examples/MTGNeighborhoods/MTGNeighborhoods.tsx (THE mechanics analog: .block > .stage + aria-hidden .track of register()'d .anchor divs; useScrollSections wiring lines 22-32, 47-76)
- src/system/examples/CrashCourseRail/CrashCourseRail.tsx (idiom analog: banner comment lines 1-2; riseStyle helper line 16; Tag = href ? 'a' : 'article' lines 130-133; Text-on-plain-wrapper .rise stagger with the load-bearing comment lines 102-115; "Read more" affordance lines 146-150; figure + Image radius="none" fill lines 134-142)
- src/system/hooks/useScrollSections.ts lines 48-93 (signature: ids → { activeId, register, scrollTo }; activeId defaults to ids[0]; effect early-returns when IntersectionObserver undefined — no no-JS guard needed in the component)
- src/system/primitives/index.ts (Section, Text, Image, Icon exports) and src/system/hooks/index.ts (useScrollSections export)
- .planning/phases/04-resident-stories-block/04-UI-SPEC.md lines 95-145 (Composition skeleton), 155-168 (primitive capability notes), 261-269 (interaction/a11y notes), 282-291 (copy is in fixtures, not here)
- .planning/phases/04-resident-stories-block/04-PATTERNS.md lines 44-130 (component pattern assignments — load-bearing)
  </read_first>
  <action>
Create `src/system/examples/ResidentStories/ResidentStories.tsx`. Start with the REQUIRED two-line banner comment (verbatim from CrashCourseRail.tsx:1-2): `// EXAMPLE — demonstrates foundations only.` then `// Replace before production with a CMS-wired block.`

Imports (prefer the `@system/*` alias per CONVENTIONS.md and the newer CrashCourseRail precedent): `import { type CSSProperties } from 'react'`; `import { Section, Text, Image, Icon } from '@system/primitives'`; `import { useScrollSections } from '@system/hooks'`; `import type { ResidentStoriesContent } from './types'`; `import s from './ResidentStories.module.css'`. (The CSS module file is authored in 04-02 — this import is expected and resolved there.)

Export `type ResidentStoriesProps = { content: ResidentStoriesContent }` and `function ResidentStories({ content }: ResidentStoriesProps)`.

Inside: `const { stories } = content`. Build `const ids = stories.map((_, i) => \`story-${i}\`)`. Call `const { activeId, register } = useScrollSections<string>(ids)` — do NOT pass `snap` (D-2: editorial sequence stays un-jacked; MTGNeighborhoods passes snap, this block must not). Compute `const activeIndex = Math.max(0, ids.indexOf(activeId))`. Add the stagger helper `const riseStyle = (d: number): CSSProperties => ({ '--rise-d': \`${d}ms\` } as CSSProperties)`.

Render `<Section theme={content.theme ?? 'pink-soft'} spacing="sm" aria-label={content.heading ?? 'Stories from people who made the move'}>` (aria-label, NOT aria-labelledby — Text exposes no id; D-8 default label is the source's). Inside Section: a `<div className={s.block}>`.

Inside `.block`, first the sticky stage `<div className={s.stage}>`:
- The progress indicator (decorative by default, D-4): `<div className={s.progress} aria-hidden style={{ ['--progress']: (activeIndex + 1) / stories.length } as CSSProperties}><span className={s.progressFill} /></div>`. Set `--progress` as an inline CSS var only — NEVER write per-frame heights in JS (the NeighborhoodPaginator `--snap-progress` idiom; the CSS scaleY transform lives in 04-02).
- Then `stories.map((story, i) => { ... })`: compute `const isActive = ids[i] === activeId` and `const Tag = story.href ? 'a' : 'article'`. Render `<Tag key={i} {...(story.href ? { href: story.href } : {})} className={[s.story, isActive && s.storyActive].filter(Boolean).join(' ')} data-side={story.side ?? (i % 2 ? 'left' : 'right')}>`. Inside: `<div className={s.inner}>` with a text column `<div className={s.text}>` and a media `<figure className={s.media}>`.
  - Text column pieces, each on a plain wrapper div carrying the stagger (Text accepts no style/id — include the load-bearing comment from CrashCourseRail.tsx:102 explaining WHY): eyebrow `<div className={s.rise} style={riseStyle(0)}><Text role="tag">{story.eyebrow}</Text></div>`; then a quote wrapper `<div className={s.quote}>` containing the decorative giant mark `<span className={s.mark} aria-hidden>&#8220;</span>` (an ornamental open-quote glyph, aria-hidden so AT does not read a stray quote — NOT an Icon) and the headline `<div className={s.rise} style={riseStyle(90)}><Text role="h2" as="h3">{story.headline}</Text></div>` (D-9: role h2 gives display-800 visual, as="h3" keeps the correct outline level → renders an `<h3>`); then attribution `<div className={s.rise} style={riseStyle(180)}><Text role="body" as="p">{story.attribution}</Text></div>`. After the pieces, the linked-only affordance: `{story.href && (<span className={s.more}>Read story <Icon name="arrow-right" size="sm" aria-hidden /></span>)}` (copy `Read story` per SPEC line 291; arrow is aria-hidden, the link names the action; do NOT add a new Icon glyph).
  - The media figure: `<figure className={s.media}><Image src={story.image.src} alt={story.image.alt} focalPoint="center" loading="lazy" radius="none" /></figure>` (radius="none" — the figure wrapper owns radius/aspect/object-fit:cover fill in 04-02 because Image ships height:auto).

After the stage, the tall scroll track `<div className={s.track} aria-hidden>` containing `stories.map((_, i) => <div key={i} ref={register(ids[i])} className={s.anchor} />)`. The track is aria-hidden because the anchors are pure scroll-distance scaffolding; the readable content lives in the stage panels (MTGNeighborhoods precedent).

Do NOT wrap the stage in `Container` (it would constrain the full-viewport sticky stage — SPEC line 158). Do NOT add `useReveal` (is-active drives the rise — SPEC line 164). Do NOT add `role="button"`/`tabindex` on any div (D-7). Do NOT add a manual `window.scroll` listener, `getBoundingClientRect` math, or a `globalProgress()` engine — `useScrollSections` is the only scroll machinery. House style: 2-space indent, single quotes, no trailing semicolons, classnames via `[...].filter(Boolean).join(' ')`. No raw hex / raw px string literals in the .tsx (banned by no-restricted-syntax in src/system/**).
  </action>
  <verify>
    <automated>grep -q "useScrollSections<string>(ids)" src/system/examples/ResidentStories/ResidentStories.tsx && grep -q "ids.indexOf(activeId)" src/system/examples/ResidentStories/ResidentStories.tsx && echo "scroll wiring present"</automated>
  </verify>
  <acceptance_criteria>
- `ResidentStories.tsx` exists and exports `ResidentStories` and `ResidentStoriesProps` (`grep -c "export function ResidentStories" ` returns 1; `grep -c "export type ResidentStoriesProps"` returns 1).
- Banner comment present: `grep -c "EXAMPLE — demonstrates foundations only" ResidentStories.tsx` returns 1.
- useScrollSections REUSED, no new hook file: `grep -q "useScrollSections<string>(ids)"` succeeds AND `ls src/system/hooks/useResidentStories*.ts src/system/examples/ResidentStories/use*.ts 2>/dev/null` lists nothing.
- NO `snap` passed (D-2): `grep -c "snap" ResidentStories.tsx` returns 0.
- NO manual scroll machinery: `grep -Ec "addEventListener\\(.scroll|getBoundingClientRect|requestAnimationFrame|globalProgress" ResidentStories.tsx` returns 0.
- Progress var wired: `grep -q "'--progress'\\]: (activeIndex + 1) / stories.length" ResidentStories.tsx` succeeds (or the equivalent inline-var expression matches `--progress`).
- Story headline uses D-9 role/level: `grep -q 'role="h2" as="h3"' ResidentStories.tsx` succeeds.
- href switch (D-7): `grep -q "story.href ? 'a' : 'article'" ResidentStories.tsx` succeeds; no fake button role: `grep -Ec 'role="button"|tabIndex' ResidentStories.tsx` returns 0.
- Accessible name via aria-label, not aria-labelledby: `grep -c "aria-label=" ResidentStories.tsx` >= 1 AND `grep -c "aria-labelledby" ResidentStories.tsx` returns 0.
- Not wrapped in Container: `grep -c "<Container" ResidentStories.tsx` returns 0.
- Track is aria-hidden and registers anchors: `grep -q "ref={register(ids\\[i\\])}" ResidentStories.tsx` succeeds.
- No raw hex/px literals: `grep -Eic "#[0-9a-f]{3,6}\\b" ResidentStories.tsx` returns 0 AND `grep -Ec "[0-9]+px" ResidentStories.tsx` returns 0.
  </acceptance_criteria>
  <done>ResidentStories renders a Section(pink-soft) → block → sticky stage (decorative aria-hidden progress driven by an inline --progress var + cross-faded story panels: real h3 headlines, href→link else article, decorative quote mark, linked-only "Read story" affordance, figure Image radius="none") → aria-hidden track of register()'d anchors. Active story derived solely from useScrollSections (no snap, no manual scroll math). All story content is in the DOM at mount (activeId defaults to ids[0]), satisfying the SC#2 no-JS baseline that 04-02's CSS keeps visible.</done>
</task>

</tasks>

<verification>
- `grep -rq "useScrollSections" src/system/examples/ResidentStories/ResidentStories.tsx` (REQ-04 / SC#3 reuse) and no new hook file exists.
- types.ts + ResidentStories.tsx exist with the contracts above (REQ-01, REQ-03 anatomy started).
- No raw hex / px string literals in either .ts/.tsx (REQ-02 enforced repo-wide; full lint gate runs in 04-02).
- Component structure guarantees all stories are in the DOM at mount (SC#2 baseline) — confirmed by the 04-02 test.
- Full-block `npm run typecheck` / `npm run lint` / tests are gated at the end of 04-02 once the CSS module, story, and barrel exist.
</verification>

<success_criteria>
- `ResidentStoriesContent`/`ResidentStory` types match the SPEC shape (optional intro eyebrow/heading per D-8, optional per-story href per D-7).
- `ResidentStories` reuses `useScrollSections` with no `snap` and no manual scroll listener (D-2), derives `activeIndex`, and wires it to both the `is-active` class and the inline `--progress` var.
- Story headlines render as `<h3>` via `Text role="h2" as="h3"` (D-9); decorative quote mark and progress are `aria-hidden`; Section carries an `aria-label`.
- Structure is no-JS-readable (all stories in the DOM, activeId = ids[0] at mount), the foundation for SC#2.
</success_criteria>

<output>
Create `.planning/phases/04-resident-stories-block/04-01-SUMMARY.md` when done.
</output>
