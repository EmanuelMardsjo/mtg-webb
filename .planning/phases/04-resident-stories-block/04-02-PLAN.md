---
phase: 04-resident-stories-block
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/system/examples/ResidentStories/ResidentStories.module.css
  - src/system/examples/ResidentStories/ResidentStories.stories.tsx
  - src/system/examples/ResidentStories/ResidentStories.test.tsx
  - src/system/examples/ResidentStories/index.ts
autonomous: true
requirements: [REQ-01, REQ-02, REQ-04, REQ-06]

must_haves:
  truths:
    - "When scroll JS runs and motion is allowed, one story is pinned in a sticky stage while others cross-fade out; a progress fill grows as the active index advances"
    - "Under prefers-reduced-motion the block is a fully-visible static stack of stories, normally scrollable, with no pinning, no cross-fade, and no progress bar"
    - "Without scroll JS / before mount the block root has NO data-live attribute, so EVERY story is in-flow opacity:1 and readable — all absolute/opacity:0 cross-fade rules are scoped behind [data-live] and never apply (SC#2)"
    - "Below 768px the block falls to the static stack (no sticky stage, no progress), regardless of the data-live gate"
    - "The block uses only tokens — no raw hex, no raw px outside sanctioned clamp()/vh/calc() layout values; the progress fill is the dark-ink --color-text-default, not the pink accent line"
    - "A Storybook story renders the 3 source residents (Default) plus a different-count story; tests pass asserting all stories visible, h3 headlines, href↔article, image alt, decorative progress, the section accessible name, and that the data-live gate is absent pre-mount and present after mount"
  artifacts:
    - path: "src/system/examples/ResidentStories/ResidentStories.module.css"
      provides: "Token-only sticky stage + track/anchor + [data-live]-gated absolute cross-faded story panels + scaleY progress fill + .rise stagger + decorative quote mark + <768px static stack + prefers-reduced-motion static-stack fallback"
      contains: "position: sticky"
    - path: "src/system/examples/ResidentStories/ResidentStories.stories.tsx"
      provides: "Default (3 residents, layout:fullscreen) + a different-count story, using image fixtures"
      contains: "layout: 'fullscreen'"
    - path: "src/system/examples/ResidentStories/ResidentStories.test.tsx"
      provides: "MockObserver-based accessibility-contract test (SC#2 baseline + data-live gate assertion, h3 level, href↔article, alt, aria-hidden progress, section name)"
      contains: "MockObserver"
    - path: "src/system/examples/ResidentStories/index.ts"
      provides: "Public barrel (named exports)"
      contains: "export { ResidentStories"
  key_links:
    - from: "src/system/examples/ResidentStories/ResidentStories.module.css [data-live] .story:not(.storyActive)"
      to: "the data-live gate the component sets after mount (04-01)"
      via: "absolute / inset:0 / opacity:0 cross-fade hidden state scoped behind [data-live]; base (no data-live) is in-flow opacity:1 (SC#2)"
      pattern: "\\[data-live\\]"
    - from: "src/system/examples/ResidentStories/ResidentStories.module.css .progressFill"
      to: "the inline --progress var set in ResidentStories.tsx"
      via: "transform: scaleY(var(--progress)); transform-origin: top"
      pattern: "scaleY\\(var\\(--progress"
    - from: "src/system/examples/ResidentStories/ResidentStories.module.css @media (prefers-reduced-motion: reduce)"
      to: "the static-stack fallback (SC#2)"
      via: "stage un-sticky/un-clip, stories opacity:1/position:relative, progress display:none"
      pattern: "prefers-reduced-motion"
---

<objective>
Turn the 04-01 component structure into the sticky cross-fading resident-story sequence, and ship the story, test, and barrel. The CSS module mirrors the proven `MTGNeighborhoods` sticky-stage + track CSS verbatim (D-10), adds the absolute cross-faded story panels (the `MTGNeighborhoods` `.cardSwap` cross-fade idiom), the `scaleY(var(--progress))` fill (the `NeighborhoodPaginator` inline-var idiom), the `.rise` stagger (CrashCourseRail idiom), and — critically — the **graceful-degradation contract (SC#2)** built on the `[data-live]` gate the component sets after mount: the base CSS state of every story (no `data-live`) is fully visible / in-flow, and ALL absolute-positioning + cross-fade hidden states are scoped behind `[data-live]`, so a no-JS / pre-mount render is a readable in-flow stack. On top of that, a `<768px` static stack and a `prefers-reduced-motion` static-stack guard (both independent of the gate). The story proves the 3 source residents and a different story count; the test asserts the full accessibility + no-JS-baseline contract including the gate.

Purpose: Delivers the visible sticky sequence + the binding graceful-degradation success criterion (REQ-04, SC#2), token purity (REQ-02), and the story + tests (REQ-06, SC#4) — completing the block anatomy (REQ-01, SC#1).

Output: `ResidentStories.module.css`, `ResidentStories.stories.tsx`, `ResidentStories.test.tsx`, `index.ts`. After this plan, full `npm run typecheck`, `npm run lint`, and the block's vitest all pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-resident-stories-block/04-UI-SPEC.md
@.planning/phases/04-resident-stories-block/04-PATTERNS.md
@src/system/examples/ResidentStories/ResidentStories.tsx
@src/system/examples/ResidentStories/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS module — sticky stage, [data-live]-gated cross-fade, progress fill, static-stack + reduced-motion degradation</name>
  <files>src/system/examples/ResidentStories/ResidentStories.module.css</files>
  <read_first>
- src/system/examples/MTGNeighborhoods/MTGNeighborhoods.module.css (VERBATIM sticky-stage source: .block lines 7-10; .stage lines 15-27; .track/.anchor lines 61-70; <768px in-flow fallback lines 75-88; reduced-motion guard lines 90-96; cross-fade .cardSwap + @keyframes card-enter lines 41-59)
- src/system/examples/CrashCourseRail/CrashCourseRail.module.css (.rise transition lines 11-15 + the load-bearing comment 8-10 "visible end-state is the base"; the hidden-pre-state-behind-a-class pattern lines 17-20; figure-fill override .media img lines 75-88; full reduced-motion guard lines 238-253; sanctioned-clamp header comment lines 1-6)
- src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css lines 44-53 (.frame: aspect-ratio + border-radius calc(var(--radius-lg) * N) + overflow:clip + surface-raised bg)
- src/system/examples/ResidentStories/ResidentStories.tsx (the contract: the `.block` root gets `data-live` ONLY after mount; non-active panels carry `.story` without `.storyActive`; active gets `.storyActive`)
- .planning/phases/04-resident-stories-block/04-UI-SPEC.md lines 174-190 (spacing tokens + radius idiom), 213-239 (color: fill = --color-text-default, track = color-mix 16% / --color-border-strong — NOT --color-accent-line), 243-256 (Motion table — the canonical reduced-motion/no-JS row 253 + the .is-live gate), 261-269 (a11y — esp. line ~267 mandating the data-live gate), 269 (<768px static stack recommendation)
- .planning/phases/04-resident-stories-block/04-PATTERNS.md lines 161-287 (CSS pattern assignments — load-bearing, esp. the SC#2-critical note at 241-249 and the progress-fill idiom 268-283)
  </read_first>
  <action>
Author `src/system/examples/ResidentStories/ResidentStories.module.css`. Open with a short header comment documenting token-only intent and the sanctioned `clamp()/vh/calc()` layout-value exception (CrashCourseRail header precedent). Classes consumed by the 04-01 component: `.block .stage .progress .progressFill .story .storyActive .inner .text .rise .quote .mark .more .media .track .anchor`.

Sticky stage (mirror MTGNeighborhoods.module.css:7-27, D-10): `.block { position: relative; padding-inline: var(--space-5); }`. `.stage { position: sticky; top: var(--space-5); height: calc(100vh - 2 * var(--space-5)); max-height: calc(100vh - 2 * var(--space-5)); overflow: hidden; border-radius: calc(var(--radius-lg) * 1.5); border: var(--border-hairline) solid var(--color-border-subtle); background: var(--color-surface-page); }` (radius nudged to `calc(var(--radius-lg) * 1.5)` for the softer source --card-radius, D-10 — token-pure).

Track + anchor (verbatim MTGNeighborhoods.module.css:61-70): `.track { display: flex; flex-direction: column; }` and `.anchor { height: 100vh; pointer-events: none; }`.

Story panels — the SC#2-CRITICAL [data-live]-gated cross-fade. This is the single, unambiguous rule set (it REPLACES the previous self-contradictory "three strategies / rely on the test" instruction — there is exactly ONE strategy: gate everything behind `[data-live]`):

1. BASE STATE (no `data-live` — this is the no-JS / pre-mount baseline AND the SC#2 contract): `.story` is a normal in-flow, fully visible, scrollable panel. Write `.story { position: relative; opacity: 1; }` (plus any base padding/min-height needed for a readable stack). NO `position: absolute`, NO `opacity: 0`, NO `inset: 0` anywhere outside a `[data-live]` (or media-query) selector. A reader with no JS sees every story stacked and readable.

2. LIVE / CROSS-FADE STATE (gated behind `[data-live]`, which the component sets only after mount): scope the absolute stacking + recede to `[data-live]`:
   - `[data-live] .stage .story { position: absolute; inset: 0; }` — stack all panels in the sticky stage (this is what enables one-at-a-time pinning).
   - `[data-live] .story:not(.storyActive) { opacity: 0; pointer-events: none; }` — non-active panels recede.
   - `[data-live] .storyActive { opacity: 1; pointer-events: auto; z-index: 1; }` — active panel on top, interactive.
   - Cross-fade feel (mirror the MTGNeighborhoods `.cardSwap` idiom): `[data-live] .story { transition: opacity var(--motion-surface-swap-duration) var(--motion-surface-swap-easing); }` and a small `translateY` on the recede if desired (token-timed). The transition lives under `[data-live]` so it never runs pre-mount.
   Because EVERY absolute/opacity:0 rule is under `[data-live]`, and the component only sets `data-live` after a post-mount effect, a no-JS / pre-mount render NEVER matches these selectors → the base in-flow opacity:1 stack holds → SC#2 satisfied. (Do NOT add an `opacity: 0` to the bare `.story` rule. Do NOT duplicate the hidden state outside the gate. There is no "rely on the test" — the gate is the mechanism.)

`.inner`: two-column layout (text + media), width-capped `width: min(1180px, 90vw)`, gap stepping `var(--space-content-gap)` → larger on wide viewports; centered in the stage (`place-items: center` / margin auto). `[data-side="left"] .inner` flips the column order (media first) — use `flex-direction: row-reverse` or `order`. `.text`: flex column, `gap: var(--space-stack-loose)`, `max-width: 30ch`. Story attribution wrapper / `.more` measure `max-width: 34ch` where relevant.

`.rise` stagger (CrashCourseRail.module.css:11-15 verbatim, incl. the "visible end-state is the base" comment): transition opacity+transform on `var(--motion-reveal-duration) var(--motion-reveal-easing) var(--rise-d, 0ms)`. The hidden pre-state must ALSO be gated so the no-JS baseline shows everything — ride it on the non-active panel WITHIN the live gate: `[data-live] .story:not(.storyActive) .rise { opacity: 0; transform: translateY(var(--space-3)); }`. With no `data-live` the `.rise` pieces stay at their visible base (opacity:1, no transform), so the static stack reads fully.

Decorative quote mark `.mark`: ornamental open-quote sized via `clamp()` (sanctioned px inside clamp), line-height ~0.62, color `var(--color-text-default)`, negative bottom margin via a `calc()` (decorative kerning). `.quote` positions the mark relative to the headline.

Media figure `.media` (FeatureSpotlight .frame idiom + CrashCourseRail fill override): `margin: 0; aspect-ratio: 4 / 5; max-width: 460px; border-radius: calc(var(--radius-lg) * 1.5); overflow: clip; background: var(--color-surface-raised);`. REQUIRED fill override: `.media img { width: 100%; height: 100%; object-fit: cover; display: block; }` (Image ships height:auto).

`.more` (linked-only affordance): `display: inline-flex; align-items: center; gap: var(--space-inline-default); color: var(--color-text-default);`.

Progress fill (D-3 inside the stage, D-4 decorative, color per SPEC line 234 + PATTERNS correction #2 — DARK INK, not the pink accent line): `.progress { position: absolute; /* left edge of stage, a thin vertical bar */ inset-block: var(--space-6); inset-inline-start: var(--space-5); width: var(--space-1); background: color-mix(in oklch, var(--color-text-default) 16%, transparent); border-radius: var(--radius-pill); overflow: hidden; }` and `.progressFill { display: block; width: 100%; height: 100%; transform: scaleY(var(--progress, 0)); transform-origin: top; background: var(--color-text-default); transition: transform var(--motion-state-change-duration) var(--motion-state-change-easing); }`. Do NOT use `--color-accent-line` (it is pink[700] in pink-soft — the fill must be the dark editorial ink). Do NOT write per-frame JS heights.

`<768px` static stack (adapt MTGNeighborhoods.module.css:75-88 — the target is a VISIBLE STACK, the same end-state as reduced-motion, and it must hold EVEN WITH `data-live` present, so qualify these rules with `[data-live]` where they override the live state OR write them to win regardless — prefer overriding both `.stage` and the gated `.story` rules): `@media (width < 768px) { .stage { position: relative; top: auto; height: auto; max-height: none; overflow: visible; } .track, .progress { display: none; } [data-live] .stage .story, .story { position: relative; inset: auto; opacity: 1; pointer-events: auto; min-height: auto; padding-block: var(--space-section-y-md); } [data-live] .story:not(.storyActive) .rise, .rise { opacity: 1; transform: none; } .inner { flex-direction: column; } .media { order: -1; } }` (media-on-top single column, SPEC line 269; the dual `[data-live] .stage .story, .story` selector ensures the static stack wins over the live gate).

`prefers-reduced-motion` guard (SC#2 canonical fallback — SPEC line 253; must win EVEN WITH `data-live`, so include the gated selectors in the override; combine MTGNeighborhoods:90-96 + CrashCourseRail:238-253): `@media (prefers-reduced-motion: reduce) { .stage { position: static; height: auto; max-height: none; overflow: visible; } .track { height: auto; } [data-live] .stage .story, .story { position: relative; inset: auto; opacity: 1 !important; pointer-events: auto; min-height: auto; padding-block: var(--space-section-y-md); transition: none !important; transform: none !important; } [data-live] .story:not(.storyActive) .rise, .rise { transition: none !important; opacity: 1 !important; transform: none !important; } .media img { transition: none !important; transform: none !important; } .progress { display: none; } }` — all stories fully visible, statically stacked, normally scrollable, no pinning/cross-fade/rise/progress. Note these guards do NOT depend on the gate to be correct: they override the gated rules so the static stack holds whether or not `data-live` is set.

Token-only: every color via `--color-*` / `color-mix(... var(--color-*) ...)`, every gap/padding via `--space-*`, every radius via `--radius-*`, every motion via `--motion-*`. No raw hex anywhere (Stylelint). The only raw px allowed are inside `clamp()`/`calc()`/`vh`/`min()` sanctioned layout values (1180px inner cap, 460px media cap, 4/5, 100vh, the quote-mark clamp). Use kebab-case class/property names.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- File exists; `npm run lint` (eslint + `stylelint "src/**/*.css"`) passes with no errors for this file.
- Sticky stage present: `grep -c "position: sticky" ResidentStories.module.css` >= 1; `grep -q "calc(100vh - 2 \\* var(--space-5))" ResidentStories.module.css` succeeds.
- Track/anchor present: `grep -q "height: 100vh" ResidentStories.module.css` succeeds.
- SC#2 [data-live] GATE present and scoping the hidden state: `grep -c "\\[data-live\\]" ResidentStories.module.css` >= 3 (stack + non-active-recede + active-restore at minimum). The absolute stacking is gated: `grep -q "\\[data-live\\] .stage .story" ResidentStories.module.css` (or `grep -Eq "\\[data-live\\][^{]*\\.story[^{]*\\{[^}]*position: absolute" via multiline check) succeeds. The non-active recede is gated: `grep -q "\\[data-live\\] .story:not(.storyActive)" ResidentStories.module.css` succeeds.
- SC#2 base state is NOT hidden: there is NO bare `.story { ... opacity: 0 ... }`. Confirm the only `opacity: 0` occurrences are under a `[data-live]` (or are the `.rise` pre-state under `[data-live]`): `grep -n "opacity: 0" ResidentStories.module.css` — manually verify every line is inside a `[data-live]`-scoped rule, never on a bare `.story` rule. There must be NO `position: absolute` on a bare (un-gated) `.story` rule: `grep -n "position: absolute" ResidentStories.module.css` — every occurrence on a `.story` must be under `[data-live]` (the only un-gated `position: absolute` allowed is `.progress`).
- Progress fill wired to the inline var as dark ink: `grep -q "scaleY(var(--progress" ResidentStories.module.css` succeeds AND `grep -q "transform-origin: top" ` succeeds AND `grep -c "--color-accent-line" ResidentStories.module.css` returns 0 (fill must NOT be the pink accent line — PATTERNS correction #2).
- Reduced-motion fallback present, static, and gate-independent: `grep -q "prefers-reduced-motion: reduce" ResidentStories.module.css` succeeds AND within that block stories are restored visible (`grep -A40 "prefers-reduced-motion" ResidentStories.module.css | grep -q "opacity: 1"`) AND it overrides the gated rule (`grep -A40 "prefers-reduced-motion" ResidentStories.module.css | grep -q "\\[data-live\\]"`).
- `<768px` static stack present and gate-independent: `grep -q "width < 768px" ResidentStories.module.css` succeeds AND within that block (`grep -A30 "width < 768px" ResidentStories.module.css | grep -q "opacity: 1"`).
- `.rise` stagger present using motion-reveal tokens: `grep -q "var(--motion-reveal-duration)" ResidentStories.module.css` succeeds; `grep -q "var(--rise-d, 0ms)" ResidentStories.module.css` succeeds.
- Media fill override present: `grep -q "object-fit: cover" ResidentStories.module.css` succeeds.
- NO raw hex: `grep -Eic "#[0-9a-f]{3,6}\\b" ResidentStories.module.css` returns 0.
- Raw px only inside sanctioned functions: every `px` occurrence is inside `clamp(`/`calc(`/`min(` (manually confirm via `grep -n "px" ResidentStories.module.css` — no bare `: NNpx;` property values).
  </acceptance_criteria>
  <done>Token-only CSS module: verbatim MTGNeighborhoods sticky stage + track/anchor (D-10), absolute cross-faded story panels whose BASE state is fully visible in-flow (SC#2) with ALL absolute/opacity:0/cross-fade hidden states scoped behind the [data-live] gate the component sets after mount, dark-ink scaleY progress fill (D-3/D-4, not the pink accent), .rise stagger (hidden pre-state also gated), decorative quote-mark clamp, 4/5 media figure with object-fit:cover fill, a <768px static stack and a prefers-reduced-motion static-stack guard that both override the gate. Lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Storybook story + barrel</name>
  <files>src/system/examples/ResidentStories/ResidentStories.stories.tsx, src/system/examples/ResidentStories/index.ts</files>
  <read_first>
- src/system/examples/CrashCourseRail/CrashCourseRail.stories.tsx (fixtures import line 4; meta + 2-story pattern lines 62-76)
- src/system/examples/MTGNeighborhoods/MTGNeighborhoods.stories.tsx (parameters: { layout: 'fullscreen' })
- src/stories/_fixtures/images.ts (confirmed keys: IMAGES.portrait, IMAGES.mtgFeatureSofa, IMAGES.mtgHeroCoat for the 3 residents; IMAGES.mtgHeroDadKid available for a 4th)
- src/system/examples/CrashCourseRail/index.ts + src/system/examples/MTGNeighborhoods/index.ts (barrel pattern — named exports only)
- .planning/phases/04-resident-stories-block/04-UI-SPEC.md lines 282-291 (the exact resident copy: Aditi/Tomás/Maya)
- .planning/codebase/CONVENTIONS.md §Module Design (barrel = named exports)
  </read_first>
  <action>
Create `ResidentStories.stories.tsx`. Import `Meta`/`StoryObj` from `@storybook/react`, `ResidentStories` from `./ResidentStories`, `type ResidentStoriesContent` from `./types`, and `IMAGES` from `../../../stories/_fixtures/images` (match the CrashCourseRail relative path). Build a local `content: ResidentStoriesContent` with the 3 source residents (SPEC lines 282-290 verbatim): Story 1 — eyebrow `Bangalore → Lindholmen`, headline `A forest twelve minutes from work.`, attribution `Aditi Menon — Staff engineer, Volvo Cars · arrived 2022 with partner & daughter`, image `IMAGES.portrait`; Story 2 — `Lisbon → Majorna` / `My mornings belong to me again.` / `Tomás Ferreira — Product designer, freelance · arrived 2023, solo`, image `IMAGES.mtgFeatureSofa`; Story 3 — `Toronto → Haga` / `I finally stopped counting the hours.` / `Maya Okonkwo — Data scientist, SKF · arrived 2021 with partner`, image `IMAGES.mtgHeroCoat`. No `heading`/`eyebrow` on content by default (D-8 — the Section aria-label carries the title). `const meta: Meta<typeof ResidentStories> = { title: 'Examples/ResidentStories', component: ResidentStories, parameters: { layout: 'fullscreen' } }`; `export default meta`; `type S = StoryObj<typeof ResidentStories>`. Export `Default: S = { args: { content } }` and a second story proving a different count — either `TwoStories: S = { args: { content: { ...content, stories: content.stories.slice(0, 2) } } }` or a `FourStories` adding `IMAGES.mtgHeroDadKid` as a 4th resident (your choice; the point is to show the progress scale changing). Optionally add one story with an `href` on a single resident to demonstrate the linked-`<a>` variant (D-7) — keep the default editorial (no href). The global Storybook reduced-motion toolbar exercises the static-stack fallback; no per-story decorator needed.

Create `index.ts` barrel (named exports only, per SPEC line 70 + CONVENTIONS.md): `export { ResidentStories, type ResidentStoriesProps } from './ResidentStories'` and `export type { ResidentStoriesContent, ResidentStory } from './types'`.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- Both files exist. `npm run typecheck` passes for the whole block (the .tsx → .module.css import from 04-01 now resolves).
- Story uses fullscreen layout: `grep -q "layout: 'fullscreen'" ResidentStories.stories.tsx` succeeds.
- Default story renders the 3 source residents: `grep -c "Aditi Menon" ResidentStories.stories.tsx` >= 1, `grep -c "Tomás Ferreira"` >= 1, `grep -c "Maya Okonkwo"` >= 1.
- Image fixtures referenced: `grep -Eq "IMAGES.(portrait|mtgFeatureSofa|mtgHeroCoat)" ResidentStories.stories.tsx` succeeds.
- A second story with a different story count exists: `grep -Ec "export const (Two|Four|Few)" ResidentStories.stories.tsx` >= 1 (or a slice/extra-resident variant present).
- Barrel exports named symbols: `grep -q "export { ResidentStories, type ResidentStoriesProps }" index.ts` succeeds AND `grep -q "export type { ResidentStoriesContent, ResidentStory }" index.ts` succeeds; no default export in index.ts (`grep -c "export default" index.ts` returns 0).
  </acceptance_criteria>
  <done>A Default story renders the 3 source residents at fullscreen and a second story proves the progress scale at a different count; the barrel exposes ResidentStories + its content types via named exports. Typecheck passes for the whole block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Accessibility-contract test (SC#2 baseline + data-live gate + anatomy)</name>
  <files>src/system/examples/ResidentStories/ResidentStories.test.tsx</files>
  <behavior>
    - All 3 stories' eyebrow + headline + attribution text render (no-IO MockObserver baseline → everything in the DOM).
    - Each story headline is an `<h3>`: getAllByRole('heading', { level: 3 }) has length === stories.length.
    - A story WITH href renders as a link with that href (getByRole('link') → toHaveAttribute('href', ...)); a story WITHOUT href is NOT a link (queryByRole('link', { name: ... }) is null).
    - Each story image alt is present (getByAltText per story, or getAllByRole('img') length === stories.length).
    - SC#2 STRUCTURAL GATE: the data-live attribute is the SOLE switch for the hidden cross-fade state. Assert (a) all story content is in the DOM under the never-firing observer (presence) AND (b) the data-live gate is correctly applied — present after the mount effect — so the test FAILS if a future edit removes the gate (turning the hidden state on unconditionally) or hard-codes the hidden state independent of the gate.
    - The progress indicator is decorative aria-hidden (an [aria-hidden] element with the progress class is present and is NOT exposed as a progressbar by default — D-4).
    - The Section has its accessible name: getByRole('region', { name: /stories from people who made the move/i }).
  </behavior>
  <read_first>
- src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx (THE analog: MockObserver stub lines 9-18; jsdom-limitation comment 20-22; href↔article assertions 67-76; "shows content without motion" baseline 102-106; heading-level + text assertions)
- .planning/codebase/TESTING.md (Vitest globals, jsdom, @testing-library; MockObserver pattern; getByRole/getByText/getAllByRole; css.modules.classNameStrategy 'non-scoped' so class names are readable; container queries)
- .planning/phases/04-resident-stories-block/04-UI-SPEC.md line 69 (the test assertion list) + lines 263-267 (h3, href↔article, decorative progress, aria-label, the data-live gate)
- src/system/examples/ResidentStories/ResidentStories.tsx + types.ts (the contract under test — note the .block root gets data-live only after the post-mount effect)
  </read_first>
  <action>
Create `ResidentStories.test.tsx`. Import `{ describe, it, expect, vi, beforeEach }` from vitest, `{ render, screen }` from `@testing-library/react`, `{ ResidentStories }` from `./ResidentStories`, `type { ResidentStoriesContent }` from `./types`. Copy the `MockObserver` stub verbatim from CrashCourseRail.test.tsx:9-18 (a class with `observe`/`unobserve`/`disconnect` = vi.fn(), assigned to `globalThis.IntersectionObserver` in `beforeEach`) — it never fires, so `activeId` stays `ids[0]` and the cross-fade hidden state is never the deciding factor; all content stays in the DOM (the SC#2 baseline). Add the jsdom-limitation comment (CrashCourseRail.test.tsx:20-22 idiom): real scroll/IO geometry + layout (`getComputedStyle`/`toBeVisible` on absolute/opacity) is unavailable in jsdom, so assert WIRING (the data-live gate) + presence, not painted visibility.

Define a local `content: ResidentStoriesContent` with 3 stories — include exactly one story WITH an `href` (e.g. story 1 `href: '#aditi'`) and the others without, to test the D-7 link↔article switch. Use small inline image fixtures (`{ src: '/x.webp', alt: '...' }` per story, distinct alts).

Tests (one `describe('ResidentStories')`):
1. renders every story's eyebrow, headline, and attribution (`screen.getByText` per piece, loop over content.stories).
2. renders each story headline as an h3 — `expect(screen.getAllByRole('heading', { level: 3 })).toHaveLength(content.stories.length)` (D-9 → as="h3").
3. renders the href story as a link with that href — `expect(screen.getByRole('link', { name: /<headline-or-eyebrow of the linked story>/i })).toHaveAttribute('href', '#aditi')`.
4. does not render a non-href story as a link — `expect(screen.queryByRole('link', { name: /<a non-linked story>/i })).toBeNull()`.
5. each story image has alt text — `expect(screen.getByAltText(content.stories[i].image.alt)).toBeInTheDocument()` per story (or `getAllByRole('img')` length check).
6. SC#2 STRUCTURAL GATE TEST (the binding one — must be capable of FAILING if the data-live gate is removed): render the component; capture `const { container } = render(...)`. (a) Assert the gate becomes present after the mount effect: `expect(container.querySelector('[data-live]')).not.toBeNull()` (the post-mount `useEffect` runs synchronously under RTL's `act`, so `data-live` is applied by the time `render` returns; if needed wrap the assertion in `await waitFor(...)`). (b) Assert the hidden cross-fade state is keyed ONLY to the gate, not hard-coded on panels: query the story panels via the readable `.story` class (classNameStrategy 'non-scoped') — `const panels = container.querySelectorAll('.story')` — assert `panels.length === content.stories.length` AND that NO non-active panel carries a hard-coded hidden marker independent of the gate (i.e. the component must not itself add an `is-hidden`/absolute class to non-active panels; only `.storyActive` + the CSS `[data-live]` gate distinguish them). Concretely assert: exactly one panel has the `storyActive` class (`container.querySelectorAll('.storyActive').length === 1`) and the others carry only `.story` (no extra hidden-state class). (c) Assert all story headlines (level 3) AND attributions render in this never-firing baseline (re-use the presence loop). Add a comment: "If a future edit removes the data-live gate or hard-codes the hidden state on non-active panels, assertions (a)/(b) fail — this is the SC#2 regression guard." This test FAILS if the gate is deleted (querySelector('[data-live]') returns null) or if the hidden state is moved off the gate onto the panels.
7. the progress indicator is decorative — assert there is an `[aria-hidden]` element (the progress) and `screen.queryByRole('progressbar')` is null by default (D-4).
8. the section has its accessible name — `expect(screen.getByRole('region', { name: /stories from people who made the move/i })).toBeInTheDocument()` (the default aria-label, D-8).

Match house test style: behavior-named `it(...)`, accessible queries first, `container`/class selectors only where there is no accessible handle (the gate + panel-class assertions in test 6 are the sanctioned `container` use). No snapshot tests.
  </action>
  <verify>
    <automated>npx vitest run src/system/examples/ResidentStories/ResidentStories.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- `npx vitest run src/system/examples/ResidentStories/ResidentStories.test.tsx` passes (all tests green).
- MockObserver stub present: `grep -c "class MockObserver" ResidentStories.test.tsx` returns 1; assigned in `beforeEach` to `globalThis ... IntersectionObserver`.
- Heading-level assertion at level 3: `grep -q "getAllByRole('heading', { level: 3 })" ResidentStories.test.tsx` succeeds (proves D-9 as="h3").
- href↔article both asserted: `grep -q "getByRole('link'" ResidentStories.test.tsx` AND `grep -q "queryByRole('link'" ResidentStories.test.tsx` both succeed.
- SC#2 STRUCTURAL GATE asserted (the regression guard, not mere text presence): `grep -q "querySelector('\\[data-live\\]')" ResidentStories.test.tsx` succeeds AND a `storyActive`-count assertion is present (`grep -Eq "querySelectorAll\\('.storyActive'\\)|\\.storyActive" ResidentStories.test.tsx`). The acceptance grep is tied to the GATE attribute, not just text: `grep -c "data-live" ResidentStories.test.tsx` >= 1.
- SC#2 baseline (presence) also asserted under the never-firing observer: `grep -ci "without motion\\|baseline\\|all stories\\|never-firing\\|data-live" ResidentStories.test.tsx` >= 1.
- Decorative progress asserted: `grep -q "queryByRole('progressbar')" ResidentStories.test.tsx` succeeds (asserts null by default — D-4).
- Section accessible name asserted: `grep -q "getByRole('region'" ResidentStories.test.tsx` succeeds with the /stories from people/i name.
- Image alt asserted: `grep -Eq "getByAltText|getAllByRole\\('img'" ResidentStories.test.tsx` succeeds.
  </acceptance_criteria>
  <done>The test renders the composed component with a never-firing MockObserver and asserts the full contract: all stories' text present (SC#2 no-JS baseline), h3 headlines (D-9), href→link / no-href→article (D-7), per-story image alt, decorative aria-hidden progress with no progressbar role (D-4), the Section's accessible name (D-8), AND the binding SC#2 structural gate — the `data-live` attribute is present after mount and is the SOLE switch for the hidden state (exactly one `.storyActive` panel, no hard-coded hidden class on non-active panels), so removing the gate makes the test fail. All tests pass.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes for the whole `ResidentStories/` folder (REQ-06).
- `npm run lint` (eslint + stylelint) passes — no raw hex, no bare-px property values (REQ-02).
- `npx vitest run src/system/examples/ResidentStories/ResidentStories.test.tsx` passes — anatomy + accessibility + SC#2 baseline + data-live gate regression guard (REQ-01, REQ-04, REQ-06; SC#2, SC#4).
- `grep -q "\[data-live\]" src/system/examples/ResidentStories/ResidentStories.module.css` and ALL absolute/opacity:0 cross-fade rules are scoped behind it; the base (no data-live) state is an in-flow opacity:1 stack (SC#2 / REQ-04).
- `grep -q "prefers-reduced-motion: reduce" src/system/examples/ResidentStories/ResidentStories.module.css` and the block restores a visible static stack under it, overriding the gate (SC#2 / REQ-04).
- `grep -q "scaleY(var(--progress" ResidentStories.module.css` and the fill is `--color-text-default`, not `--color-accent-line` (color contract).
- The full DS test suite is unaffected: `npm test` passes.
</verification>

<success_criteria>
- A sticky-stories block exists under `src/system/examples/ResidentStories/` with full folder anatomy (tsx, types, css, stories, test, index) — SC#1.
- Scroll-driven progression works (sticky stage + cross-fade + scaleY progress driven by the active index) and degrades to a fully-visible static stack under no-JS / pre-mount (no data-live gate → base in-flow stack), prefers-reduced-motion, and <768px — SC#2 (binding).
- Uses only tokens/primitives; reuses `useScrollSections` (no new hook) — SC#3.
- Storybook story renders (Default 3 residents + a different count) and tests pass, including the data-live gate regression guard — SC#4.
</success_criteria>

<output>
Create `.planning/phases/04-resident-stories-block/04-02-SUMMARY.md` when done.
</output>
