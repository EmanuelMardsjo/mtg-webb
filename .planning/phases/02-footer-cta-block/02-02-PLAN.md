---
phase: 02-footer-cta-block
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/system/examples/FooterCta/FooterCta.module.css
  - src/system/examples/FooterCta/FooterCta.stories.tsx
  - src/system/examples/FooterCta/FooterCta.test.tsx
  - src/system/examples/FooterCta/index.ts
autonomous: true
requirements: [REQ-01, REQ-02, REQ-05, REQ-06]
must_haves:
  truths:
    - "The CTA pill is a 2px outlined, large-radius, transparent, centered column built only from tokens"
    - "The filled CTA button uses text-default-on-surface-page inversion (AA-safe) and lifts on hover"
    - "Footer columns lay out as 1.6fr 1fr 1fr 1fr collapsing responsively, built from block-local CSS Grid"
    - "Reveal stagger animates the pill subtree and is fully static under prefers-reduced-motion"
    - "A Storybook story renders the block (Default amber + amber-deep) and Vitest tests pass"
  artifacts:
    - path: "src/system/examples/FooterCta/FooterCta.module.css"
      provides: "Block-local styling: outlined pill, filled button, runway frame, footer grid, reveal, reduced-motion guard"
      min_lines: 60
    - path: "src/system/examples/FooterCta/FooterCta.stories.tsx"
      provides: "Default + amber-deep stories with seeded copy"
      contains: "Examples/FooterCta"
    - path: "src/system/examples/FooterCta/FooterCta.test.tsx"
      provides: "Vitest suite with IntersectionObserver mock"
      contains: "MockObserver"
    - path: "src/system/examples/FooterCta/index.ts"
      provides: "Public barrel"
      exports: ["FooterCta", "FooterCtaProps", "FooterCtaContent"]
  key_links:
    - from: "src/system/examples/FooterCta/FooterCta.module.css"
      to: ".ctaPill:not(.revealed) .rise"
      via: "reveal pre-state scoped to the pill wrapper"
      pattern: "ctaPill:not\\(.revealed\\)"
    - from: "src/system/examples/FooterCta/FooterCta.stories.tsx"
      to: "src/stories/_fixtures/images.ts"
      via: "IMAGES.mtgHeroCouch runway media"
      pattern: "IMAGES\\.mtgHeroCouch"
---

<objective>
Complete the FooterCta block: the CSS Module (token-only styling for the outlined CTA pill, filled button, runway media frame, block-local footer grid, reveal stagger, reduced-motion guard), the Storybook story (Default amber + amber-deep variants with seeded copy), the Vitest test suite (IntersectionObserver mock + faithful assertions), and the barrel export.

Mirror `src/system/examples/FeatureSpotlight/` file-for-file, applying the corrections from 02-PATTERNS.md: reveal class lives on `.ctaPill` (not `.root`), footer columns are block-local CSS Grid, drop parallax-only CSS, copy the IntersectionObserver `MockObserver` stub verbatim.

Purpose: Make the block visually faithful to variant a, token-pure (REQ-02), and verified (REQ-06).
Output: `FooterCta.module.css`, `FooterCta.stories.tsx`, `FooterCta.test.tsx`, `index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-footer-cta-block/02-UI-SPEC.md
@.planning/phases/02-footer-cta-block/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/02-footer-cta-block/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author FooterCta.module.css (token-only, all corrections)</name>
  <files>src/system/examples/FooterCta/FooterCta.module.css</files>
  <read_first>
    - src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css (analog — copy .rise transition, filled .cta button, reduced-motion block, frame radius trick; DROP all parallax CSS)
    - .planning/phases/02-footer-cta-block/02-PATTERNS.md (FooterCta.module.css Pattern Assignments lines 157-225; Additional Corrections 1-3 + 5)
    - .planning/phases/02-footer-cta-block/02-UI-SPEC.md (Spacing Scale lines 182-199; Color lines 226-256; Motion lines 259-269; Interaction Notes lines 273-280)
    - src/system/examples/FooterCta/FooterCta.tsx (the class names this file must define: root, ctaPill, revealed, rise, ctaButton, runwayMedia, footWordmark, footCols, footCol, footBar, coords)
  </read_first>
  <action>
    Create `FooterCta.module.css`. NO raw hex, NO raw px anywhere (Stylelint + the eslint no-restricted-syntax gate enforce this in src/system/**). All values flow through `--space-*`, `--radius-*`, `--color-*`, `--motion-*`, `--border-*` tokens. Use `s` idioms from the analog. Define:

    `.root`: layout wrapper only (e.g. `display: block`). CORRECTION 5 — do NOT add `--parallax-*` vars or `.editorial` transform (no parallax in variant a).

    `.rise` + reveal pre-state (copy the analog's transition verbatim) but CORRECTION 3 — scope the hidden pre-state to the pill wrapper: `.ctaPill:not(.revealed) .rise { opacity: 0; transform: translateY(var(--space-3)); }` (NOT `.root:not(.revealed)`). Keep "visible is base, hidden is pre-state" so the no-IO test baseline renders content.

    `.ctaPill`: centered flex column — `display: flex; flex-direction: column; align-items: center; text-align: center; gap: var(--space-stack-loose)` (24px); `border: var(--border-strong) solid var(--color-border-strong)`; `border-radius: calc(var(--radius-lg) * 4)` (64px — the sanctioned non-token large-radius trick, analog precedent); `background: transparent` (the amber Section shows through); `padding: var(--space-9) var(--space-7)` (96/48). Responsive: under 768px step padding down (e.g. `var(--space-7) var(--space-5)`); under 560px `border-radius: calc(var(--radius-lg) * 2)` (32px). Apply the spec measure constraints on the pill text where appropriate (title max-width ~16ch via a child selector or accept the centered column width; lead max-width ~46ch) using `ch` units (allowed — `ch` is not `px`).

    `.ctaButton`: copy the analog's filled `.cta` block — `display: inline-flex; align-items: center; gap: var(--space-inline-default); padding: var(--space-3) var(--space-5); border-radius: var(--radius-pill); background: var(--color-text-default); color: var(--color-surface-page); text-decoration: none;` with the opacity+transform transition via `--motion-hover-duration`/`--motion-hover-easing`. Hover: `@media (hover: hover) { .ctaButton:hover { transform: translateY(-2px); } }` (spec specifies -2px).

    `.runwayMedia`: `border-radius: calc(var(--radius-lg) * 2)` (32px); `overflow: clip`; `background: var(--color-surface-raised)`; `aspect-ratio: 16 / 7`; `margin: 0`. `.runwayMedia img { width: 100%; height: 100%; object-fit: cover; display: block; }`. Under 768px `aspect-ratio: 4 / 5`. (CORRECTION 2 — the ratio/radius live here, not on Image.)

    `.footWordmark` + `.footCol ul`: list reset (`list-style: none; margin: 0; padding: 0`). `.footWordmark` is a block-level decorative link; size its inline SVG via `currentColor` and a `ch`/`%`/`rem`-based width (no px). Add top spacing before the columns via `--space-stack-xl`.

    `.footCols`: CORRECTION 4 — block-local CSS Grid: `display: grid; grid-template-columns: 1.6fr 1fr 1fr 1fr; gap: var(--space-stack-xl) var(--space-content-gap);` Responsive: `@media (width < 860px) { grid-template-columns: 1fr 1fr; }` with the first column spanning full width (`.footCol:first-child { grid-column: 1 / -1; }` inside that breakpoint); `@media (width < 560px) { grid-template-columns: 1fr; }`. `.footCol ul { gap: var(--space-stack-default); display: flex; flex-direction: column; }` (16px).

    `.footBar`: `display: flex; flex-wrap: wrap; gap: var(--space-inline-default); border-top: var(--border-subtle) solid var(--color-border-strong); padding-top: var(--space-5);` (use a token, not px). `.coords { font-variant-numeric: tabular-nums; }` (push to the end with `margin-left: auto` is fine — no px).

    Reduced-motion guard (CORRECTION 5 — drop the parallax-only `.editorial`/`.frame` resets; keep only `.rise` + `.ctaButton`): `@media (prefers-reduced-motion: reduce) { .rise { transition: none !important; opacity: 1 !important; transform: none !important; } .ctaButton { transition: none !important; } }` (REQ-04 graceful degradation).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx stylelint "src/system/examples/FooterCta/FooterCta.module.css"</automated>
  </verify>
  <acceptance_criteria>
    - `npx stylelint src/system/examples/FooterCta/FooterCta.module.css` passes (no raw hex)
    - no raw px literals: `grep -nE "[0-9]+px" src/system/examples/FooterCta/FooterCta.module.css` returns nothing (REQ-02)
    - no raw hex: `grep -nE "#[0-9a-fA-F]{3,8}" src/system/examples/FooterCta/FooterCta.module.css` returns nothing (REQ-02)
    - reveal pre-state scoped to the pill: `grep -q "ctaPill:not(.revealed) .rise" src/system/examples/FooterCta/FooterCta.module.css` (CORRECTION 3)
    - footer grid is the asymmetric track: `grep -q "1.6fr 1fr 1fr 1fr" src/system/examples/FooterCta/FooterCta.module.css` (CORRECTION 4)
    - large radius via calc: `grep -q "calc(var(--radius-lg)" src/system/examples/FooterCta/FooterCta.module.css`
    - reduced-motion guard present: `grep -q "prefers-reduced-motion" src/system/examples/FooterCta/FooterCta.module.css` (REQ-04)
    - no parallax CSS: `grep -c "parallax" src/system/examples/FooterCta/FooterCta.module.css` returns 0 (CORRECTION 5)
  </acceptance_criteria>
  <done>CSS module styles the outlined pill, filled button, runway frame, block-local footer grid, reveal stagger (scoped to .ctaPill), and reduced-motion guard — entirely from tokens; stylelint + the px/hex grep gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Storybook story (Default amber + amber-deep) and barrel</name>
  <files>src/system/examples/FooterCta/FooterCta.stories.tsx, src/system/examples/FooterCta/index.ts</files>
  <read_first>
    - src/system/examples/FeatureSpotlight/FeatureSpotlight.stories.tsx (analog — meta title, layout: 'fullscreen', Default + second themed story, IMAGES import path)
    - src/system/examples/FeatureSpotlight/index.ts (analog — copy verbatim, rename)
    - .planning/phases/02-footer-cta-block/02-UI-SPEC.md (Copywriting Contract lines 286-302 — seed all copy verbatim; story notes line 70)
    - src/stories/_fixtures/images.ts (confirm key `mtgHeroCouch` exists for runway media)
  </read_first>
  <action>
    Create `index.ts` (barrel) by copying the analog and renaming: `export { FooterCta, type FooterCtaProps } from './FooterCta'` and `export type { FooterCtaContent } from './types'` (REQ-01 anatomy).

    Create `FooterCta.stories.tsx` mirroring the analog. Import `Meta, StoryObj` from `@storybook/react`; `FooterCta` from `./FooterCta`; `type FooterCtaContent` from `./types`; `IMAGES` from `../../../stories/_fixtures/images`. Build a `content: FooterCtaContent` constant seeded VERBATIM from the UI-SPEC Copywriting Contract: runway eyebrow `№ 09 — Before you go`, runway headline `You've read the stories.\nThe last step is the easy one.` (literal `\n` line break before "The"), runway lead, runway media `IMAGES.mtgHeroCouch` (sofa image matching source feature-sofa.png); cta eyebrow `№ 10 — When you're ready`, cta title `What kind of life\nare you looking for?` (line break before "are"), cta lead, cta action `{ label: 'Begin with one line', href: '#' }`; footer columns 1-4 with tags/items exactly per the contract (column 1 items plain, column 2 items as links, column 4 phone item as link), footer legal `© Move to Gothenburg · 2026 · A non-profit relocation companion`, footer coords `57.7089°N · 11.9746°E`. meta: `title: 'Examples/FooterCta'`, `component: FooterCta`, `parameters: { layout: 'fullscreen' }`. Export `Default: StoryObj` with `args: { content }` (amber footer-zone by default). Export a second story `AmberDeep: StoryObj` with `args: { content: { ...content, footerTheme: 'amber-deep' } }` (mirrors the analog's `BlueSoftTheme`, maps the source `[data-amber="deep"]` toggle). Reduced-motion is handled by the global Storybook toolbar decorator — do not add per-story motion config.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx eslint src/system/examples/FooterCta/FooterCta.stories.tsx src/system/examples/FooterCta/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes; eslint passes for both files
    - `grep -q "Examples/FooterCta" src/system/examples/FooterCta/FooterCta.stories.tsx`
    - `grep -q "layout: 'fullscreen'" src/system/examples/FooterCta/FooterCta.stories.tsx`
    - `grep -q "IMAGES.mtgHeroCouch" src/system/examples/FooterCta/FooterCta.stories.tsx` (runway media fixture)
    - `grep -q "footerTheme: 'amber-deep'" src/system/examples/FooterCta/FooterCta.stories.tsx` (second story)
    - `grep -q "Begin with one line" src/system/examples/FooterCta/FooterCta.stories.tsx` (verbatim CTA copy)
    - barrel exports all three: `grep -q "FooterCtaProps" src/system/examples/FooterCta/index.ts && grep -q "FooterCtaContent" src/system/examples/FooterCta/index.ts`
  </acceptance_criteria>
  <done>Default (amber) and AmberDeep stories render the block fullscreen with seeded copy + mtgHeroCouch media; barrel exposes FooterCta, FooterCtaProps, FooterCtaContent; typecheck + eslint pass.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest test suite (IntersectionObserver mock + faithful assertions)</name>
  <files>src/system/examples/FooterCta/FooterCta.test.tsx</files>
  <read_first>
    - src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx (analog — copy MockObserver + beforeEach verbatim; extend the 5 assertions)
    - .planning/codebase/TESTING.md (Vitest, Testing Library, accessible queries, classNameStrategy non-scoped, MockObserver pattern)
    - .planning/phases/02-footer-cta-block/02-UI-SPEC.md (test assertions line 71)
    - src/system/examples/FooterCta/types.ts (FooterCtaContent shape for the inline fixture)
  </read_first>
  <action>
    Create `FooterCta.test.tsx`. CORRECTION 7 — copy the `MockObserver` class and the `beforeEach` IntersectionObserver stub VERBATIM from the analog (`useReveal` constructs an IO in an effect; jsdom lacks it). Import `{ describe, it, expect, vi, beforeEach }` from vitest, `{ render, screen }` from `@testing-library/react`, `FooterCta` from `./FooterCta`, `type FooterCtaContent` from `./types`.

    Build a local `content: FooterCtaContent` fixture (small, self-contained — does NOT need the full story copy, but must include: runway eyebrow/headline/lead + media; cta eyebrow/title/lead/action with a known href like `/start` and label like `Begin with one line`; footer with 2 columns where at least one item has an href and one does not; legal string; coords). Reuse the analog's inline-constant style (TESTING.md fixtures section).

    Assertions (extend the analog's 5; per UI-SPEC line 71):
    - runway eyebrow, headline, lead present via `screen.getByText`
    - cta eyebrow, title, lead present via `screen.getByText`
    - CTA button is a link with the supplied href + accessible name: `const link = screen.getByRole('link', { name: /begin with one line/i }); expect(link).toHaveAttribute('href', '/start')`
    - CTA title renders as `<h2>`: `expect(screen.getByRole('heading', { level: 2 })).toHaveTextContent(content.cta.title)`
    - runway headline renders as `<h1>`: `expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent(content.runway.headline)`
    - footer column tags + items render (assert a tag string and a link item + a non-link item by text)
    - footer legal copy renders via `screen.getByText`
    - content visible without motion (no-IO baseline): render and assert the cta title is in the document (the hidden pre-state is CSS-only, never applied in jsdom)
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx vitest run src/system/examples/FooterCta/FooterCta.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/system/examples/FooterCta/FooterCta.test.tsx` passes (all assertions green)
    - `grep -q "class MockObserver" src/system/examples/FooterCta/FooterCta.test.tsx` (CORRECTION 7 — IO stub verbatim)
    - `grep -q "level: 2" src/system/examples/FooterCta/FooterCta.test.tsx` AND `grep -q "level: 1" src/system/examples/FooterCta/FooterCta.test.tsx` (h2 CTA title + h1 runway headline)
    - `grep -q "getByRole('link'" src/system/examples/FooterCta/FooterCta.test.tsx` (CTA accessible-name + href assertion)
    - test asserts the no-IO baseline (content present without intersection firing)
  </acceptance_criteria>
  <done>Vitest suite passes: runway/cta text present, CTA is a named link with href, CTA title is h2, runway headline is h1, footer columns/legal render, content visible without motion.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes for the whole block.
- `npm run lint` passes (ESLint + Stylelint — no raw hex/px in src/system/**).
- `npx vitest run src/system/examples/FooterCta/FooterCta.test.tsx` passes.
- All 9 corrections enforced: reveal scoped to `.ctaPill`, block-local footer grid (no Grid/Col), no parallax CSS, MockObserver verbatim, no hex/px, amber + amber-deep stories, mtgHeroCouch fixture, one filled CTA.
</verification>

<success_criteria>
- Full folder anatomy exists under `src/system/examples/FooterCta/` (REQ-01).
- Block uses only tokens/primitives — no raw hex/px; lint passes (REQ-02).
- Visually faithful to CTA pill variant a: outlined transparent pill, one filled CTA, runway frame, amber field (REQ-05).
- Storybook story (Default + amber-deep) renders the block and Vitest tests pass (REQ-06).
</success_criteria>

<output>
Create `.planning/phases/02-footer-cta-block/02-02-SUMMARY.md` when done.
</output>
