---
phase: 02-footer-cta-block
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/system/examples/FooterCta/types.ts
  - src/system/examples/FooterCta/FooterCta.tsx
autonomous: true
requirements: [REQ-01, REQ-03, REQ-05]
must_haves:
  truths:
    - "A FooterCta content type exposes the runway + CTA + footer CMS shape"
    - "FooterCta renders a neutral runway lead-in above an amber footer zone"
    - "The block shows exactly one dominant filled CTA button inside an outlined pill"
    - "The CTA pill contents reveal on scroll via useReveal and are visible at mount under reduced motion"
    - "Footer columns, wordmark, legal bar, and optional coords render from content"
  artifacts:
    - path: "src/system/examples/FooterCta/types.ts"
      provides: "FooterCtaContent CMS-shaped type"
      contains: "FooterCtaContent"
    - path: "src/system/examples/FooterCta/FooterCta.tsx"
      provides: "FooterCta example block component"
      exports: ["FooterCta", "FooterCtaProps"]
      min_lines: 50
  key_links:
    - from: "src/system/examples/FooterCta/FooterCta.tsx"
      to: "useReveal"
      via: "ref attached to the .ctaPill wrapper div"
      pattern: "useReveal\\(\\)"
    - from: "src/system/examples/FooterCta/FooterCta.tsx"
      to: "src/system/examples/FooterCta/types.ts"
      via: "import type FooterCtaContent"
      pattern: "FooterCtaContent"
---

<objective>
Create the FooterCta example block: the CMS content type (`FooterCtaContent`) and the React component that composes a neutral "runway" lead-in `Section` above an `amber` "footer-zone" `Section` containing one dominant outlined CTA pill plus footer chrome (wordmark, multi-column links, legal bar). The component wires `useReveal()` to the CTA-pill subtree for a restrained staggered rise.

This is an ADAPTATION phase. The dominant instruction is: mirror `src/system/examples/FeatureSpotlight/` file-for-file, applying the load-bearing corrections from 02-PATTERNS.md (drop `useParallax`, footer columns are block-local CSS not `Grid`/`Col`, reveal class on `.ctaPill` not `.root`, `aria-label` not `aria-labelledby`, no arbitrary `Image aspect`, two static themed sections — no scroll color engine).

Purpose: Establish the runtime shape of the block so Plan 02 can style, story, and test it.
Output: `src/system/examples/FooterCta/types.ts`, `src/system/examples/FooterCta/FooterCta.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-footer-cta-block/02-UI-SPEC.md
@.planning/phases/02-footer-cta-block/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define FooterCtaContent type</name>
  <files>src/system/examples/FooterCta/types.ts</files>
  <read_first>
    - src/system/examples/FeatureSpotlight/types.ts (exact analog — copy the `import type { ThemeName } from '@tokens/themes'` pattern and the `{Component}Content` naming)
    - .planning/phases/02-footer-cta-block/02-UI-SPEC.md (lines 76-103 — the authoritative FooterCtaContent shape)
    - src/tokens/themes/index.ts (confirm `ThemeName` is exported and includes 'neutral', 'amber', 'amber-deep')
  </read_first>
  <action>
    Create `types.ts` exporting `type FooterCtaContent` exactly per UI-SPEC lines 79-103 (REQ-03, CONVENTIONS.md `{ComponentName}Content` rule). Import `ThemeName` from `@tokens/themes`. Shape: `runway` object `{ eyebrow: string; headline: string; lead: string; media?: { src: string; alt: string } }` (media OPTIONAL per D-4); `cta` object `{ eyebrow: string; title: string; lead: string; action: { label: string; href: string } }`; `footer` object `{ columns: Array<{ tag: string; items: Array<{ text: string; href?: string }> }>; legal: string; coords?: string }`; plus optional `runwayTheme?: ThemeName` and `footerTheme?: ThemeName`. Match the file style in CONVENTIONS.md (2-space indent, single quotes, no trailing semicolons). Do NOT add fields beyond the spec shape.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes
    - `grep -q "export type FooterCtaContent" src/system/examples/FooterCta/types.ts`
    - `grep -q "media?:" src/system/examples/FooterCta/types.ts` (runway media optional, D-4)
    - `grep -q "coords?:" src/system/examples/FooterCta/types.ts` (footer coords optional)
    - `grep -q "@tokens/themes" src/system/examples/FooterCta/types.ts` (ThemeName imported, no inline string union)
  </acceptance_criteria>
  <done>`FooterCtaContent` exports the nested runway/cta/footer shape with optional media, coords, runwayTheme, footerTheme; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build FooterCta component (runway + amber footer zone + CTA pill + footer chrome)</name>
  <files>src/system/examples/FooterCta/FooterCta.tsx</files>
  <read_first>
    - src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx (file-for-file analog — banner comment, riseStyle idiom, plain-wrapper stagger, filled-pill `<a>`, figure+Image frame)
    - .planning/phases/02-footer-cta-block/02-PATTERNS.md (Pattern Assignments for FooterCta.tsx, lines 30-145; Additional Corrections lines 310-322)
    - .planning/phases/02-footer-cta-block/02-UI-SPEC.md (Composition block lines 105-156; Primitive Capability Notes lines 160-177)
    - src/system/primitives/Section.tsx (confirm: theme, spacing union sm|md|lg|xl, spreads ...rest so aria-label forwards, NO ref forwarding)
    - src/system/primitives/Text.tsx (confirm: no style/id/...rest; defaultTag map display->h1, h2->h2, lead->p, tag/meta/button->span)
    - src/system/primitives/TextLink.tsx (renders `<a>`, spreads anchor attrs — used for footer link items)
    - src/system/hooks/useReveal.ts (returns { ref, revealed }; revealed=true at mount under reduced motion)
    - src/system/examples/MTGTopbar.tsx (decorative wordmark-inside-`<a aria-label>` with aria-hidden mark — footer wordmark pattern)
  </read_first>
  <action>
    Create `FooterCta.tsx` mirroring `FeatureSpotlight.tsx`, applying ALL 9 corrections.

    Banner comment first two lines verbatim from the analog (`// EXAMPLE — demonstrates foundations only.` / `// Replace before production with a CMS-wired block.`) per CONVENTIONS.md.

    Imports (external -> @system -> relative order): `{ type CSSProperties }` from react; `{ Section, Container, Stack, Text, TextLink, Image, Icon }` from `@system/primitives` (ADD `TextLink`, the analog lacks it); `{ useReveal }` from `@system/hooks` — CORRECTION 5: do NOT import `useParallax`; `type { FooterCtaContent }` from `./types`; `s` from `./FooterCta.module.css`. Export `type FooterCtaProps = { content: FooterCtaContent }`.

    In the component: call `useReveal()` once, destructure `{ ref: revealRef, revealed }`. CORRECTION: attach `revealRef` DIRECTLY to the `.ctaPill` div — NO mergedRef callback (the analog's mergedRef exists only for parallax; drop it entirely). Keep `riseStyle = (d: number): CSSProperties => ({ '--rise-d': \`${d}ms\` } as CSSProperties)`.

    Return `<div className={s.root}>` wrapping TWO stacked `Section`s (CORRECTION 6 — two static themed sections, NO scroll color engine, NO document/html mutation):
    1) RUNWAY: `<Section theme={content.runwayTheme ?? 'neutral'} spacing="lg">` > `<Container size="default">` > `<Stack gap="loose">` containing `<Text role="tag">{runway.eyebrow}</Text>`, `<Text role="hero" as="h1">{runway.headline}</Text>` (page h1, fs-4xl), `<Text role="lead">{runway.lead}</Text>`. The runway text is NOT reveal-staggered. After the Stack, guard-render the optional media (D-4): `{content.runway.media && (<figure className={s.runwayMedia}><Image src alt focalPoint="center" loading="lazy" /></figure>)}`. CORRECTION 2: do NOT pass `aspect=` or arbitrary `radius` to `Image` — the 16/7 ratio + large radius live on the `.runwayMedia` figure in CSS (Plan 02); `Image` fills via object-fit.
    2) FOOTER ZONE: `<Section theme={content.footerTheme ?? 'amber'} spacing="xl" aria-label={content.cta.title}>` (CORRECTION 1: aria-label on the Section — NOT aria-labelledby; `Section` forwards ...rest, `Text` has no id). Inside `<Container size="default">`:
       - The CTA pill: `<div ref={revealRef} className={[s.ctaPill, revealed && s.revealed].filter(Boolean).join(' ')}>`. Children are plain wrapper `<div className={s.rise} style={riseStyle(N)}>` carrying the stagger (CORRECTION 1 — stagger rides plain divs, Text takes no style): eyebrow `<Text role="tag">` at 0ms; title `<Text role="display" as="h2">` at 90ms (h2, fs-5xl); lead `<Text role="lead">` at 180ms; then the filled CTA `<a href={content.cta.action.href} className={[s.ctaButton, s.rise].join(' ')} style={riseStyle(270)}>` containing `<Text role="button" as="span">{content.cta.action.label}</Text>` and `<Icon name="arrow-right" size="md" />` (CORRECTION 8 — exactly ONE dominant filled CTA; author as raw `<a>`, NOT TextLink, per D-2). CORRECTION 8: NO glow/watermark element.
       - Footer wordmark: `<a className={s.footWordmark} href="#" aria-label="Move to Gothenburg — home">` with a decorative inline SVG marked `aria-hidden` (mirror MTGTopbar). The SVG may be a simple `currentColor` wordmark path/text; keep it `aria-hidden` since the `<a>` carries the label.
       - Footer columns: CORRECTION 4 — `<div className={s.footCols}>` (block-local CSS Grid, NOT `Grid`/`Col` primitives — those cannot express `1.6fr 1fr 1fr 1fr`). Map `content.footer.columns` to `<div className={s.footCol} key={i}>` each with `<Text role="meta" as="p">{col.tag}</Text>` and a `<ul>` of `<li key={j}>` rendering `it.href ? <TextLink href={it.href}>{it.text}</TextLink> : <Text role="body-sm" as="span">{it.text}</Text>`.
       - Footer bar: `<div className={s.footBar}>` with `<Text role="meta" as="span">{content.footer.legal}</Text>` and guarded `{content.footer.coords && <Text role="meta" as="span" className={s.coords}>{content.footer.coords}</Text>}`.

    Use `.filter(Boolean).join(' ')` class composition per CONVENTIONS.md line 40. Guard all optional content (media, coords) with conditional rendering; never throw during render (CONVENTIONS.md line 69). All class names reference `s.*` from the CSS module that Plan 02 creates — they will resolve once Plan 02 ships; typecheck/lint do not require the CSS file to exist for the `.tsx` to compile (CSS Modules import is typed loosely).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run typecheck && npx eslint src/system/examples/FooterCta/FooterCta.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes; `npx eslint src/system/examples/FooterCta/FooterCta.tsx` passes
    - `grep -q "useReveal" src/system/examples/FooterCta/FooterCta.tsx` AND component attaches the ref to the CTA pill
    - `grep -c "useParallax" src/system/examples/FooterCta/FooterCta.tsx` returns 0 (CORRECTION 5 — no parallax import)
    - `grep -c "aria-labelledby" src/system/examples/FooterCta/FooterCta.tsx` returns 0 (CORRECTION 1 — aria-label only)
    - `grep -q "aria-label=" src/system/examples/FooterCta/FooterCta.tsx` (footer-zone Section + wordmark labelled)
    - footer columns do NOT use Grid/Col: `grep -E "import .*(Grid|Col)" src/system/examples/FooterCta/FooterCta.tsx` returns nothing (CORRECTION 4)
    - `Image` is rendered WITHOUT an `aspect=` prop: `grep -v '^[[:space:]]*//' src/system/examples/FooterCta/FooterCta.tsx | grep -c "aspect=" ` returns 0 (CORRECTION 2)
    - exactly one filled CTA anchor exists (`grep -c "s.ctaButton" src/system/examples/FooterCta/FooterCta.tsx` returns 1; CORRECTION 8)
    - banner comment present: `head -2 src/system/examples/FooterCta/FooterCta.tsx | grep -q "EXAMPLE"`
  </acceptance_criteria>
  <done>FooterCta renders runway (neutral) + footer-zone (amber) with one outlined CTA pill (revealRef attached, staggered children), footer columns (block-local div grid), wordmark, and legal bar; typecheck + eslint pass; all corrections enforced by grep gates.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes for the new files.
- `npx eslint src/system/examples/FooterCta/FooterCta.tsx` passes (no raw hex/px in .tsx, hooks rules satisfied).
- Component composes exactly two themed Sections and one dominant filled CTA.
- All 9 PATTERNS/UI-SPEC corrections verified via grep gates above (no useParallax, no aria-labelledby, no Image aspect=, no Grid/Col for footer, reveal on pill, one CTA, no glow/watermark, static themes, optional media guarded).
</verification>

<success_criteria>
- `FooterCtaContent` type and `FooterCta` component exist under `src/system/examples/FooterCta/` (REQ-01 folder anatomy started, REQ-03 content prop type).
- Component is visually structured for variant a: runway lead-in -> one dominant CTA pill -> quiet footer chrome (REQ-05 faithful structure).
- useReveal wired to the CTA-pill subtree; reduced-motion safe by construction (revealed=true at mount).
</success_criteria>

<output>
Create `.planning/phases/02-footer-cta-block/02-01-SUMMARY.md` when done.
</output>
