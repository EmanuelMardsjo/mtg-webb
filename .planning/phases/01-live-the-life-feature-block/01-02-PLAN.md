---
phase: 01-live-the-life-feature-block
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/system/examples/FeatureSpotlight/types.ts
  - src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx
  - src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css
  - src/system/examples/FeatureSpotlight/FeatureSpotlight.stories.tsx
  - src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx
  - src/system/examples/FeatureSpotlight/index.ts
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03, REQ-04, REQ-06]
must_haves:
  truths:
    - "A FeatureSpotlight example block exists under src/system/examples/ with the full 6-file folder anatomy"
    - "The block renders eyebrow, headline (as <h2>), lead, a single filled-pill CTA with an arrow icon, and a media frame image — composed from @system primitives only"
    - "The block reveals its content on scroll via useReveal and applies restrained parallax via useParallax; under prefers-reduced-motion it is fully static and content is visible"
    - "Content is supplied via a FeatureSpotlightContent prop type (CMS-shaped) with an optional theme that applies locally to the Section (no document.body mutation)"
    - "No raw hex / no raw px in the block's .ts/.tsx or .module.css; lint passes"
    - "A Storybook story renders the block (default pink-soft + one alternate theme); Vitest tests pass"
  artifacts:
    - path: "src/system/examples/FeatureSpotlight/types.ts"
      provides: "FeatureSpotlightContent CMS content shape"
      contains: "export type FeatureSpotlightContent"
    - path: "src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx"
      provides: "The composed example block component"
      contains: "export function FeatureSpotlight"
      min_lines: 30
    - path: "src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css"
      provides: "Block-local CSS: frame clip/aspect/radius, rise stagger, filled pill, reduced-motion + parallax guards"
      contains: "prefers-reduced-motion"
    - path: "src/system/examples/FeatureSpotlight/FeatureSpotlight.stories.tsx"
      provides: "Default + alternate-theme stories using seeded copy and a fixture image"
      contains: "title: 'Examples/FeatureSpotlight'"
    - path: "src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx"
      provides: "Render + text/alt/href assertions, content-visible-without-motion baseline"
      contains: "describe('FeatureSpotlight'"
    - path: "src/system/examples/FeatureSpotlight/index.ts"
      provides: "Barrel exporting component, props, and content type"
      contains: "export { FeatureSpotlight"
  key_links:
    - from: "src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx"
      to: "@system/hooks (useReveal + useParallax)"
      via: "merged-ref callback attaching both refs to one plain root element"
      pattern: "useReveal|useParallax"
    - from: "src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css"
      to: "--parallax-text/-frame/-img (written by useParallax)"
      via: "transform: translate3d(0, var(--parallax-*, 0px), 0)"
      pattern: "var\\(--parallax-"
    - from: "src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx"
      to: "content.theme"
      via: "Section theme prop (local, no body mutation)"
      pattern: "theme=\\{content.theme"
---

<objective>
Build the `FeatureSpotlight` example block — a themeable, image-led editorial feature section with a media frame that reveals on scroll (`useReveal`) and drifts with restrained parallax (`useParallax` from plan 01). It adapts `mtg-feature` into the DS idiom: `Section` -> `Container` -> centered editorial `Stack` (eyebrow / headline / lead / filled-pill CTA) plus a clipped media `figure`. Theme is local via the `Section theme` prop (no ambient body mutation). Content is CMS-shaped via `FeatureSpotlightContent`. The block uses only `@tokens` + `@system` primitives, ships a Storybook story and passing tests, and is fully static under `prefers-reduced-motion`.

This plan delivers REQ-01 (folder anatomy), REQ-02 (tokens/primitives only), REQ-03 (Content prop type), REQ-04 (hook-driven motion + reduced-motion), REQ-06 (story + tests).

Purpose: Establishes the MTG block-import pattern (adapt, not copy) that phases 2-4 reuse.
Output: The 6-file `src/system/examples/FeatureSpotlight/` folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-the-life-feature-block/01-UI-SPEC.md
@.planning/phases/01-live-the-life-feature-block/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/system/examples/EditorialText/EditorialText.tsx
@src/system/examples/MTGHero/MTGHero.module.css
@src/system/hooks/useReveal.ts
@src/stories/_fixtures/images.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define FeatureSpotlightContent type and the block component</name>
  <files>src/system/examples/FeatureSpotlight/types.ts, src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx</files>
  <read_first>
    - src/system/examples/EditorialText/types.ts (the {ComponentName}Content shape with optional ThemeName)
    - src/system/examples/EditorialText/EditorialText.tsx (the { content } + Section->Container->Stack->Text composition idiom + banner comment)
    - src/system/examples/MTGHero/MTGHero.tsx (attaching a ref to a PLAIN element — heroRef on <main>, line ~36 — the pattern for the merged reveal+parallax root)
    - src/system/examples/JourneyPanel/JourneyPanel.tsx (the --rd / --rise-d inline CSSProperties stagger idiom, lines ~67-68, ~106-108)
    - src/system/hooks/useReveal.ts (returns { ref, revealed }; revealed=true at mount under reduced motion)
    - src/system/primitives/Section.tsx, Container.tsx, Stack.tsx, Text.tsx, Image.tsx, Icon.tsx (verify prop contracts — Section/Container do NOT forward ref; Text has no id/style passthrough; Image.aspect is a named-token union only)
    - .planning/phases/01-live-the-life-feature-block/01-PATTERNS.md (section "FeatureSpotlight.tsx" — verified primitive contracts + corrections 3/4/5)
    - .planning/phases/01-live-the-life-feature-block/01-UI-SPEC.md (Component Anatomy, Typography role map, Copywriting Contract)
  </read_first>
  <action>
    Create `src/system/examples/FeatureSpotlight/types.ts` mirroring `EditorialText/types.ts`: `import type { ThemeName } from '@tokens/themes'` then `export type FeatureSpotlightContent = { eyebrow: string; headline: string; lead: string; cta: { label: string; href: string }; image: { src: string; alt: string }; theme?: ThemeName }` (REQ-03 — exact shape from UI-SPEC).

    Create `src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx`. Start with the verbatim two-line banner comment used by every example block (`// EXAMPLE — demonstrates foundations only.` / `// Replace before production with a CMS-wired block.`). Imports in alias order: `import { type CSSProperties } from 'react'`; `import { Section, Container, Stack, Text, Image, Icon } from '@system/primitives'`; `import { useReveal, useParallax } from '@system/hooks'`; `import type { FeatureSpotlightContent } from './types'`; `import s from './FeatureSpotlight.module.css'`. Export `type FeatureSpotlightProps = { content: FeatureSpotlightContent }` and `export function FeatureSpotlight({ content }: FeatureSpotlightProps)`.

    Inside: call `const { ref: revealRef, revealed } = useReveal()` and `const { ref: parallaxRef } = useParallax()`. Per PATTERNS correction 5, define an inline merged-ref callback `const mergedRef = (el: HTMLDivElement | null) => { revealRef.current = el; parallaxRef.current = el }` — do NOT add a shared merge-ref utility. Per PATTERNS correction 4, the merged ref must attach to a PLAIN ref-bearing element (Section/Container do not forward refs). Mirror MTGHero: render the outermost block element as a plain `<div ref={mergedRef} className={[s.root, revealed && s.revealed].filter(Boolean).join(' ')}>` and place `Section` inside it. Per PATTERNS correction 3, give that root `aria-label={content.headline}` (accessible name equivalent — do NOT attempt aria-labelledby->headline-id; the Text primitive exposes no id).

    Stagger helper: `const riseStyle = (d: number): CSSProperties => ({ '--rise-d': \`${d}ms\` } as CSSProperties)` (JourneyPanel idiom; ms strings are allowed in .tsx — only px/hex literals are banned).

    Compose inside the root div: `<Section theme={content.theme ?? 'pink-soft'} spacing="lg">` -> `<Container size="default">` -> `<Stack gap="loose" align="center" className={s.editorial}>` containing: `<Text role="tag" className={s.rise} style={riseStyle(0)}>{content.eyebrow}</Text>`; `<Text role="display" as="h2" className={s.rise} style={riseStyle(90)}>{content.headline}</Text>` (MUST pass `as="h2"` — role="display" defaults to h1, a11y note requires h2); `<Text role="lead" className={s.rise} style={riseStyle(180)}>{content.lead}</Text>`; the CTA as a filled-pill anchor `<a href={content.cta.href} className={[s.cta, s.rise].join(' ')} style={riseStyle(270)}>` wrapping `<Text role="button" as="span">{content.cta.label}</Text>` and `<Icon name="arrow-right" size="md" />`. After the Stack (still inside Container): `<figure className={[s.frame, s.rise].join(' ')} style={riseStyle(360)}>` containing `<Image src={content.image.src} alt={content.image.alt} focalPoint="center" loading="lazy" />`. Per PATTERNS correction 2, do NOT pass an `aspect` prop to Image (its union cannot express 1312/611 or 4/5 — the ratio is set in CSS on `.frame`).

    Note: if Text role="display"/role="button"/role="lead"/role="tag" do not accept `style`/`className` exactly as written, confirm against Text.tsx before adjusting; PATTERNS confirms Text accepts `className` but not `style`/`id`. If Text rejects inline `style`, move the `--rise-d` style to the nearest accepting wrapper (the .rise class still drives the transition); document the choice in the SUMMARY. The headline `aria` accessible name comes from the root `aria-label`, so no headline id is needed.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npx eslint src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx src/system/examples/FeatureSpotlight/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes.
    - `npx eslint` on both files passes (no raw px/hex; the only inline dynamic value is the `--rise-d` ms string).
    - `types.ts` contains `export type FeatureSpotlightContent` with all of: eyebrow, headline, lead, cta, image, optional theme.
    - `FeatureSpotlight.tsx` contains the banner comment, `export function FeatureSpotlight`, both `useReveal` and `useParallax` calls, a single inline merged-ref callback attached to a plain `<div>`, `theme={content.theme ?? 'pink-soft'}`, `Text role="display" as="h2"`, exactly one `<a className={...s.cta...}>` CTA with `<Icon name="arrow-right" .../>`, and `aria-label={content.headline}` on the root.
    - No `aspect=` prop is passed to `Image` (grep: `grep -c 'aspect=' src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx` returns 0).
    - No aria-labelledby (grep: `grep -c 'aria-labelledby' src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx` returns 0).
  </acceptance_criteria>
  <done>The type and component compile and lint clean, composing the source layout from primitives with hook-driven reveal+parallax wired through one merged-ref root, theme local to Section, headline as h2, single filled-pill CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Author the block CSS module (frame, rise stagger, parallax + reduced-motion guards, filled pill)</name>
  <files>src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css</files>
  <read_first>
    - src/system/examples/MTGHero/MTGHero.module.css (CSS-var consumption + translate3d, the "card radius" calc(var(--radius-lg) * 2), .pillSend inverted control, and the @media (prefers-reduced-motion: reduce) guard block)
    - src/tokens/generated/tokens.css (confirm token names: --radius-lg, --radius-pill, --color-surface-raised, --color-surface-page, --color-text-default, --space-inline-default, --space-3, --space-5, --motion-reveal-duration, --motion-reveal-easing, --motion-hover-duration, --motion-hover-easing)
    - .planning/phases/01-live-the-life-feature-block/01-PATTERNS.md (section "FeatureSpotlight.module.css" — exact CSS to mirror + corrections 1 and 2)
    - .planning/codebase/CONVENTIONS.md (kebab-case classes/custom-props; no raw hex/px; CSS-var dynamic values)
  </read_first>
  <action>
    Create `src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css`. Declare default parallax vars on the root so SSR/no-JS is static: `.root { --parallax-text: 0px; --parallax-frame: 0px; --parallax-img: 0px; }` (mirrors MTGHero `.card { --mx:0px; --my:0px }`). Consume them: `.editorial { transform: translate3d(0, var(--parallax-text, 0px), 0); }`, `.frame { transform: translate3d(0, var(--parallax-frame, 0px), 0); }`, `.frame img { transform: translate3d(0, var(--parallax-img, 0px), 0); }`.

    Media frame (PATTERNS correction 1 + 2): `.frame { border-radius: calc(var(--radius-lg) * 2); overflow: clip; background: var(--color-surface-raised); aspect-ratio: 1312 / 611; margin: 0; }` and `.frame img { width: 100%; height: 100%; object-fit: cover; display: block; }`. Add `@media (width < 768px) { .frame { aspect-ratio: 4 / 5; } }`. Do NOT use `var(--card-radius)` (it does not exist).

    Rise stagger (visible end-state is base; hidden pre-state only before reveal): `.rise { transition: opacity var(--motion-reveal-duration) var(--motion-reveal-easing) var(--rise-d, 0ms), transform var(--motion-reveal-duration) var(--motion-reveal-easing) var(--rise-d, 0ms); }`. Hidden pre-state guarded by the absence of `.revealed` on the root, e.g. `.root:not(.revealed) .rise { opacity: 0; transform: translateY(var(--space-3)); }`. Adjust the selector to match the actual root/revealed class structure from Task 1 (principle: visible = base, hidden only pre-reveal so no-JS shows content).

    Filled-pill CTA (D-2, accent-by-inversion): `.cta { display: inline-flex; align-items: center; gap: var(--space-inline-default); padding: var(--space-3) var(--space-5); border-radius: var(--radius-pill); background: var(--color-text-default); color: var(--color-surface-page); text-decoration: none; transition: opacity var(--motion-hover-duration) var(--motion-hover-easing), transform var(--motion-hover-duration) var(--motion-hover-easing); }` and `@media (hover: hover) { .cta:hover { opacity: 0.9; transform: translateY(-1px); } }`.

    Optional `.editorial` measure constraints if useful (max-width via existing tokens only). Reduced-motion guard (REQ-04 + success criterion 2): `@media (prefers-reduced-motion: reduce) { .rise { transition: none !important; opacity: 1 !important; transform: none !important; } .editorial, .frame, .frame img { transform: none !important; } }`. Every color/radius/spacing value must be a token var — NO raw hex, NO raw px except the `0px` fallbacks inside `var(...)` and within `translateY()` which use tokens; if Stylelint flags the `aspect-ratio` unitless ratios or the `0px` literals, keep them (ratios are unitless and `0px` inside var fallbacks is the in-repo MTGHero precedent — confirm `npm run lint` passes).
  </action>
  <verify>
    <automated>npx stylelint src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `npx stylelint src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css` passes (no raw hex).
    - File contains `calc(var(--radius-lg) * 2)` (NOT `var(--card-radius)`; grep: `grep -c 'card-radius' …` returns 0).
    - File contains `aspect-ratio: 1312 / 611` and a `@media (width < 768px)` rule setting `aspect-ratio: 4 / 5`.
    - File contains `var(--parallax-text` , `var(--parallax-frame`, `var(--parallax-img` consumed in `translate3d`.
    - File contains a `@media (prefers-reduced-motion: reduce)` block neutralizing `.rise` transitions and `.editorial/.frame` transforms.
    - `.cta` uses `background: var(--color-text-default)` and `color: var(--color-surface-page)` (accent-by-inversion) and `border-radius: var(--radius-pill)`.
  </acceptance_criteria>
  <done>The CSS module compiles via Stylelint with token-only values, sets the media ratio in CSS, consumes the parallax vars, staggers the rise, styles the inverted pill, and goes fully static under reduced motion.</done>
</task>

<task type="auto">
  <name>Task 3: Story, test, and barrel — complete the folder anatomy</name>
  <files>src/system/examples/FeatureSpotlight/FeatureSpotlight.stories.tsx, src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx, src/system/examples/FeatureSpotlight/index.ts</files>
  <read_first>
    - src/system/examples/EditorialText/EditorialText.stories.tsx (default + theme-variant story shape)
    - src/system/examples/MTGHero/MTGHero.stories.tsx (importing IMAGES fixture via relative ../../../stories/_fixtures/images; optional parameters.layout)
    - src/system/examples/MTGHero/MTGHero.test.tsx (render + getByText/getByAltText/href assertions)
    - src/system/examples/EditorialText/EditorialText.test.tsx (minimal { content } render)
    - src/system/examples/MTGHero/index.ts (barrel named-export style)
    - src/stories/_fixtures/images.ts (IMAGES shape { src, alt } — use a warm editorial image, e.g. IMAGES.portrait or IMAGES.water)
    - .planning/phases/01-live-the-life-feature-block/01-UI-SPEC.md (Copywriting Contract — exact seeded copy)
    - .planning/phases/01-live-the-life-feature-block/01-PATTERNS.md (sections "FeatureSpotlight.stories.tsx", "FeatureSpotlight.test.tsx", "index.ts")
    - .planning/codebase/TESTING.md (jsdom, screen queries, no observer mock needed)
  </read_first>
  <action>
    Create `src/system/examples/FeatureSpotlight/FeatureSpotlight.stories.tsx`: `import type { Meta, StoryObj } from '@storybook/react'`; `import { FeatureSpotlight } from './FeatureSpotlight'`; `import { IMAGES } from '../../../stories/_fixtures/images'`. `const meta: Meta<typeof FeatureSpotlight> = { title: 'Examples/FeatureSpotlight', component: FeatureSpotlight }`; `export default meta`; `type S = StoryObj<typeof FeatureSpotlight>`. Author a `content` object using the EXACT seeded copy from UI-SPEC Copywriting Contract: eyebrow `№ 01 — A life, not just a job`; headline `Live the life you've been saving for later.` (preserve the intended line break before "been" — use `\n` in the string per the content shape note, or keep the single string if line-break handling is CSS `text-wrap: balance`); the full lead paragraph; cta `{ label: 'Explore the life', href: '#' }`; image from a warm fixture (e.g. `IMAGES.portrait`). Export a `Default: S` story (theme defaults to pink-soft) and one alternate-theme story (e.g. pass a different valid ThemeName via `content.theme`). Do NOT add a reduced-motion story — the global Storybook toolbar decorator handles it. Optionally set `parameters: { layout: 'fullscreen' }` if a full-bleed preview reads better.

    Create `src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx` mirroring MTGHero.test.tsx: `import { describe, it, expect } from 'vitest'`; `import { render, screen } from '@testing-library/react'`; `import { FeatureSpotlight } from './FeatureSpotlight'`; `import type { FeatureSpotlightContent } from './types'`. Build a local `const content: FeatureSpotlightContent` with eyebrow/headline/lead/cta/image (cta.href e.g. `/life`, image alt e.g. `A warm domestic scene`). In `describe('FeatureSpotlight', …)` assert: eyebrow, headline, and lead text are present via `screen.getByText`; the image alt via `screen.getByAltText`; the CTA via `screen.getByRole('link', { name: /explore the life/i })` (or `getByText(...).closest('a')`) and assert its `href` `toContain`/`toBe` the expected value; and a content-visible-without-motion baseline — assert the headline text node is `toBeInTheDocument()` (jsdom has no IntersectionObserver and the CSS hidden pre-state is CSS-only/not applied in jsdom, so content is found). Do NOT mock IntersectionObserver (MTGHero does not).

    Create `src/system/examples/FeatureSpotlight/index.ts` mirroring MTGHero/index.ts (named exports, no semicolons): `export { FeatureSpotlight, type FeatureSpotlightProps } from './FeatureSpotlight'` and `export type { FeatureSpotlightContent } from './types'`.
  </action>
  <verify>
    <automated>npx vitest run src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx` passes.
    - The test asserts eyebrow + headline + lead text, image alt, and the CTA link href.
    - `FeatureSpotlight.stories.tsx` contains `title: 'Examples/FeatureSpotlight'`, a `Default` story, and an alternate-theme story; imports `IMAGES` from the fixtures.
    - `index.ts` exports `FeatureSpotlight`, `FeatureSpotlightProps`, and `FeatureSpotlightContent`.
    - `npm run typecheck` and `npm run lint` both pass for the whole `src/system/examples/FeatureSpotlight/` folder (REQ-02 no hex/px; REQ-06 tests pass).
  </acceptance_criteria>
  <done>The folder has all 6 files; the story renders the block in default + alternate theme; tests pass; lint + typecheck clean — completing REQ-01/03/06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author/CMS content -> block render | `FeatureSpotlightContent` (eyebrow/headline/lead/cta.href/image) is author-supplied. In this DS it comes from fixtures/Storybook; in production it would be CMS-authored. |
| browser scroll events -> useParallax | untrusted timing/volume of scroll events drive rAF writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `cta.href` rendered into an `<a href>` | accept | Static editorial example block; href is author-controlled, not user-input. No `javascript:`-scheme sanitization added here (out of scope for a DS example; flagged for the future CMS-wiring layer noted in the banner comment). |
| T-01-02 | Denial of Service | scroll listener in `useParallax` | mitigate | Single rAF-throttled passive listener; `cancelAnimationFrame` on each frame and on cleanup; no per-frame React state. Disabled entirely under reduced motion. |
| T-01-03 | Information Disclosure | image `src` is a remote Unsplash URL (fixtures) | accept | Fixtures only; production replaces with licensed/self-hosted assets per the fixture file banner. No PII. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this phase — both plans use only existing deps (react, @system, @tokens, @storybook, vitest already in package.json). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run lint` — ESLint + Stylelint clean across the FeatureSpotlight folder (no raw hex/px).
- `npm run typecheck` — no type errors.
- `npx vitest run src/system/examples/FeatureSpotlight/FeatureSpotlight.test.tsx` — block tests pass.
- Manual (Storybook, optional): `npm run storybook` renders `Examples/FeatureSpotlight` in default + alternate theme; reduced-motion toolbar makes it static.
</verification>

<success_criteria>
- A FeatureSpotlight block exists under `src/system/examples/` with the full 6-file anatomy (REQ-01).
- It composes from `@system` primitives + `@tokens` only; lint passes with no raw hex/px (REQ-02).
- Content is supplied via `FeatureSpotlightContent` with local `theme` (REQ-03).
- Motion uses `useReveal` + `useParallax`; the block is fully static and content-visible under `prefers-reduced-motion` (REQ-04).
- A Storybook story renders the block (default + alternate theme) and Vitest tests pass (REQ-06).
</success_criteria>

<output>
Create `.planning/phases/01-live-the-life-feature-block/01-02-SUMMARY.md` when done.
</output>
