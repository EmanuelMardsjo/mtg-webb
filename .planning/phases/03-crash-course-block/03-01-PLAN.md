---
phase: 03-crash-course-block
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/system/examples/CrashCourseRail/types.ts
  - src/system/examples/CrashCourseRail/CrashCourseRail.tsx
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03, REQ-04, REQ-05]
must_haves:
  truths:
    - "A reader sees an editorial header (heading, optional eyebrow + lead) and a horizontal strip of topic cards."
    - "A reader can scroll the rail sideways; cards snap to the start edge."
    - "A reader can jump to any card with a labelled dot button; the active dot reads as current to assistive tech."
    - "A reader can step the rail with Previous/Next buttons that disable at the ends."
    - "A keyboard user can focus the rail and move it with Arrow keys and Home/End."
    - "A card with a href is a navigable link; a card without one is a non-interactive article (no dead click)."
    - "Under prefers-reduced-motion the rail jumps instantly (no smooth scroll) and remains fully usable."
  artifacts:
    - path: "src/system/examples/CrashCourseRail/types.ts"
      provides: "CrashCourseRailContent + CrashCourseRailCard content shape (CMS-shaped)"
      contains: "export type CrashCourseRailContent"
    - path: "src/system/examples/CrashCourseRail/CrashCourseRail.tsx"
      provides: "CrashCourseRail component: header, scroll-snap rail, dot nav, prev/next, component-local activeIndex + keyboard"
      exports: ["CrashCourseRail", "CrashCourseRailProps"]
      min_lines: 90
  key_links:
    - from: "src/system/examples/CrashCourseRail/CrashCourseRail.tsx"
      to: "railRef.scrollLeft"
      via: "passive rAF-throttled scroll listener → setActiveIndex"
      pattern: "scrollLeft"
    - from: "src/system/examples/CrashCourseRail/CrashCourseRail.tsx"
      to: "scrollToIndex"
      via: "dot/prev/next onClick + rail onKeyDown"
      pattern: "scrollToIndex"
    - from: "src/system/examples/CrashCourseRail/CrashCourseRail.tsx"
      to: "@system/hooks useReveal"
      via: "header wrapper ref + revealed class gate"
      pattern: "useReveal"
---

<objective>
Author the `CrashCourseRail` content type and the React component (variant C — the "tram window" scroll-snap card rail). This plan delivers the full interactive surface: editorial header wired to `useReveal`, a horizontal scroll-snap rail of cards (`<a href>` when linkable, else non-interactive `<article>`), a labelled dot `<nav>` with `aria-current`, round prev/next buttons that disable at the ends, and the component-local active-index tracking + `scrollToIndex` + keyboard handlers. The styling lives in plan 03-02; this plan references the CSS-module class names by string so the markup is complete.

Purpose: Establish the rail interaction (the phase's enduring deliverable per ROADMAP Phase 3 goal + SC#2 accessibility), faithfully adapting the source's reduced-motion/no-JS rail as the canonical block (D-3 deck-fan dropped, D-4 drawer out of scope).
Output: `types.ts` and `CrashCourseRail.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crash-course-block/03-UI-SPEC.md
@.planning/phases/03-crash-course-block/03-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define CrashCourseRailContent type</name>
  <files>src/system/examples/CrashCourseRail/types.ts</files>
  <read_first>
    - src/system/examples/FooterCta/types.ts (exact `{Component}Content` idiom; `{ src; alt }` media shape; `theme?: ThemeName` import from @tokens/themes)
    - .planning/phases/03-crash-course-block/03-UI-SPEC.md (lines 74-92, the locked content shape)
    - .planning/phases/03-crash-course-block/03-PATTERNS.md (lines 186-208, types analog)
  </read_first>
  <action>
    Create `types.ts` exporting two types. `CrashCourseRailCard` = { title: string; description: string; image: { src: string; alt: string }; href?: string }. The `href` is OPTIONAL per D-4 (present → navigable card; absent → non-interactive article). `CrashCourseRailContent` = { eyebrow?: string; heading: string; lead?: string; cards: CrashCourseRailCard[]; theme?: ThemeName }. Import `ThemeName` as a type-only import from `@tokens/themes` (verified export path, mirrors FooterCta runwayTheme/footerTheme). Match the FooterCta file style exactly: single quotes, no trailing semicolons, 2-space indent. No `style`/`id` fields. Keep the `{ src; alt }` media shape verbatim from FooterCta/types.ts so `Image` alt stays required.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes.
    - `grep -c "export type CrashCourseRailContent" src/system/examples/CrashCourseRail/types.ts` returns 1.
    - `grep -c "export type CrashCourseRailCard" src/system/examples/CrashCourseRail/types.ts` returns 1.
    - `grep -q "href?: string" src/system/examples/CrashCourseRail/types.ts` (optional href present, D-4).
    - `grep -q "import type { ThemeName } from '@tokens/themes'" src/system/examples/CrashCourseRail/types.ts`.
  </acceptance_criteria>
  <done>Both types compile and export; the card carries an optional `href` and a required `{ src; alt }` image.</done>
</task>

<task type="auto">
  <name>Task 2: Author CrashCourseRail component (header + rail + dots + prev/next + active-index + keyboard)</name>
  <files>src/system/examples/CrashCourseRail/CrashCourseRail.tsx</files>
  <read_first>
    - src/system/examples/FooterCta/FooterCta.tsx (banner comment lines 1-2; `useReveal` + `riseStyle` lines 12-16; plain-wrapper `--rise-d` stagger + the "Text does not accept style/id" comment lines 49-58; whole-element-as-link anchor lines 59-67; block-local inline `<svg aria-hidden focusable="false">` lines 71-83)
    - src/system/examples/FeatureSpotlight/FeatureSpotlight.tsx (revealRef on inner div + `[s.head, revealed && s.revealed].filter(Boolean).join(' ')` gate)
    - src/system/examples/MTGNeighborhoods/NeighborhoodPaginator.tsx (SHAPE ONLY: rafRef guard lines 35,100-103; `{ passive: true }` listener line 107; cancelAnimationFrame cleanup lines 109-113; `typeof window === 'undefined'` guard line 41; `<nav aria-label>` line 123-125; `aria-current={isActive ? 'true' : undefined}` line 144; `aria-label={\`Jump to ${n.name}\`}` line 145; `onClick` line 147)
    - src/system/hooks/useReveal.ts (matchMedia reduced-motion check lines 3-7 shape; useReveal returns { ref, revealed }, ref is HTMLDivElement)
    - src/system/primitives/index.ts (confirm Section, Container, Stack, Text, Image, Icon are exported)
    - .planning/phases/03-crash-course-block/03-UI-SPEC.md (Component Anatomy lines 96-152; Interaction & Accessibility lines 274-280; Motion lines 256-268; D-2/D-4/D-5/D-6 lines 322-326)
    - .planning/phases/03-crash-course-block/03-PATTERNS.md (component pattern assignments lines 51-181; the active-index/scrollToIndex/keyboard sketches)
  </read_first>
  <action>
    Create `CrashCourseRail.tsx`. Start with the two-line banner comment verbatim from FooterCta.tsx ("// EXAMPLE — demonstrates foundations only." + "// Replace before production with a CMS-wired block.") per CONVENTIONS.md.

    Imports (external → alias → relative order): from `react` import `type CSSProperties, useEffect, useRef, useState, useCallback`; from `@system/primitives` import `Section, Container, Stack, Text, Image, Icon`; from `@system/hooks` import `useReveal`; type-only import `CrashCourseRailContent` from `./types`; `s` from `./CrashCourseRail.module.css`. Export `type CrashCourseRailProps = { content: CrashCourseRailContent }`.

    HEADER + REVEAL: call `useReveal()` once → `{ ref: revealRef, revealed }`. Define `riseStyle = (d: number): CSSProperties => ({ '--rise-d': \`${d}ms\` } as CSSProperties)` (verbatim FooterCta idiom). Wrap the header in a NATIVE `<div ref={revealRef} className={[s.head, revealed && s.revealed].filter(Boolean).join(' ')}>` — the ref + `.revealed` gate live on this native div because `Stack` has NO `ref` prop and `Section` does not forward refs (mirror FeatureSpotlight.tsx exactly). Inside that div render a `Stack gap="loose" align="center"` carrying ONLY its layout className (no ref, no revealed gate). Inside, carry the `--rise-d` stagger on plain `<div className={s.rise} style={riseStyle(N)}>` wrappers (NOT on Text — include the "Text does not accept style/id" comment from FooterCta): eyebrow at 0ms (only when `content.eyebrow`, `<Text role="tag">`); heading at 90ms (`<Text role="display" as="h2">{content.heading}</Text>`); lead at 180ms (only when `content.lead`, `<Text role="lead">`). Heading is the document `<h2>` (`as="h2"`, source title is an h2).

    COMPONENT-LOCAL ACTIVE INDEX (D-5, NO new hook): `const railRef = useRef<HTMLUListElement | null>(null)`, `const [activeIndex, setActiveIndex] = useState(0)`, `const rafRef = useRef<number | null>(null)`. In a `useEffect` (deps `[]`): guard `if (!railRef.current || typeof window === 'undefined') return`. Define `tick()` that clears `rafRef.current = null`, reads the rail's `firstElementChild` as the cell, derives `step` from `cell.offsetWidth` plus the gap (read the gap from a second child's `offsetLeft - firstChild.offsetLeft - firstChild.offsetWidth`, or `getComputedStyle` gap — do NOT hardcode the clamp px), and `setActiveIndex(Math.round(rail.scrollLeft / step))` clamped to `[0, cards.length - 1]`. Define `onScroll()` that early-returns if `rafRef.current !== null` else `rafRef.current = window.requestAnimationFrame(tick)`. Attach `rail.addEventListener('scroll', onScroll, { passive: true })`; cleanup removes the listener and `cancelAnimationFrame` (mirror NeighborhoodPaginator lines 100-113).

    SCROLLTOINDEX (reduced-motion guard): compute `reduce` inline via `typeof window !== 'undefined' && window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches` (useReveal shape). `const scrollToIndex = useCallback((i: number) => { ... })` deps `[content.cards.length]`: clamp `i` to `[0, cards.length - 1]`, read `rail.children[clamped]` as the cell, call `rail.scrollTo({ left: cell.offsetLeft, behavior: reduce ? 'auto' : 'smooth' })`. Under reduced motion this is an instant jump (REQ-04).

    KEYBOARD (SC#2): `const onRailKeyDown = (e) => {}` handling `ArrowRight`/`ArrowLeft` → `scrollToIndex(activeIndex ± 1)`, `Home` → `scrollToIndex(0)`, `End` → `scrollToIndex(cards.length - 1)`; call `e.preventDefault()` on handled keys only.

    RAIL MARKUP: `<ul ref={railRef} className={s.rail} tabIndex={0} aria-label={\`${content.heading} — scrollable cards\`} onKeyDown={onRailKeyDown}>`. Map `content.cards` to `<li key={i} className={s.cell}>`. Inside each li, choose the element: `const Tag = card.href ? 'a' : 'article'` (capitalized const so JSX treats it as a component). Render `<Tag {...(card.href ? { href: card.href } : {})} className={s.card}>` containing: `<figure className={s.media}><Image src={card.image.src} alt={card.image.alt} focalPoint="center" loading="lazy" radius="none" /></figure>`; then `<span className={s.cap}>` with `<Text role="h3" as="span" className={s.cardTitle}>{card.title}</Text>` (role h3 as span per D-6), `<Text role="body-sm" as="span" className={s.cardDesc}>{card.description}</Text>`, and ONLY when `card.href` a `<span className={s.more}>Read more <Icon name="arrow-right" size="sm" aria-hidden /></span>`. Do NOT add `font-weight: 600` anywhere — exactly 2 weights (400/800), drop the source `.ccx__more` weight 600 (D-6).

    CONTROL ROW: `<div className={s.controls}>` containing (1) a dot `<nav className={s.dots} aria-label="Crash course pagination">` (D-2 — a labelled nav, NOT role="tablist") mapping cards to `<button type="button" key={i} className={[s.dot, i === activeIndex && s.dotActive].filter(Boolean).join(' ')} aria-current={i === activeIndex ? 'true' : undefined} aria-label={\`Go to ${card.title}\`} onClick={() => scrollToIndex(i)} />`; and (2) a `<div className={s.nav}>` with two round `<button type="button" className={s.navBtn}>`: Previous with `aria-label="Previous"`, `disabled={activeIndex === 0}`, `onClick={() => scrollToIndex(activeIndex - 1)}`, containing a block-local inline chevron-left `<svg viewBox="0 0 24 24" aria-hidden focusable="false">…</svg>` (Icon has no chevron glyph — author it locally per FooterCta wordmark SVG precedent); Next with `aria-label="Next"`, `disabled={activeIndex === content.cards.length - 1}`, `onClick={() => scrollToIndex(activeIndex + 1)}`, a chevron-right SVG. The card "Read more" arrow uses `Icon name="arrow-right"`; prev/next chevrons are bespoke SVGs.

    COMPOSITION + EMPTY STATE: wrap everything in `<Section theme={content.theme ?? 'yellow'} spacing="lg" aria-label={content.heading}>` → `<Container size="default">`. The header Stack always renders; the rail + controls render ONLY when `content.cards.length > 0` (empty state: header alone, no rail, no controls — SPEC line 303, mirrors FooterCta conditional-content guarding). `aria-label={content.heading}` on the Section (Text exposes no `id`, so `aria-labelledby` is not wired — Phase 1/2 precedent).

    Match the codebase style: single quotes, no trailing semicolons, 2-space indent, classnames via `[...].filter(Boolean).join(' ')`. NO raw hex, NO raw px anywhere in the .tsx (ESLint `no-restricted-syntax` bans them in src/system/**). The chevron SVG path coordinates inside `viewBox`/`d` attributes are SVG geometry, not CSS px literals — keep them inside the SVG markup (FooterCta wordmark SVG precedent uses numeric SVG attrs and passes lint).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes and `npm run lint` passes (no raw hex/px in src/system/**).
    - Banner comment present: `grep -q "EXAMPLE — demonstrates foundations only" src/system/examples/CrashCourseRail/CrashCourseRail.tsx`.
    - Dot nav is a labelled nav, NOT a tablist (D-2): `grep -q 'aria-label="Crash course pagination"' src/...CrashCourseRail.tsx` AND `grep -c 'role="tablist"' src/...CrashCourseRail.tsx` returns 0.
    - aria-current present on active dot: `grep -q "aria-current=" src/...CrashCourseRail.tsx`.
    - Component-local tracking present, NO new hook file created: `grep -q "scrollLeft" src/...CrashCourseRail.tsx` AND `grep -q "addEventListener('scroll'" src/...CrashCourseRail.tsx` AND `grep -q "{ passive: true }" src/...CrashCourseRail.tsx` AND there is NO new file under `src/system/hooks/` (this plan's files_modified contains no hook file).
    - scrollToIndex + reduced-motion guard: `grep -q "scrollToIndex" src/...CrashCourseRail.tsx` AND `grep -q "prefers-reduced-motion: reduce" src/...CrashCourseRail.tsx` AND `grep -q "behavior:" src/...CrashCourseRail.tsx`.
    - Keyboard handler present (SC#2): `grep -q "onKeyDown" src/...CrashCourseRail.tsx` AND grep finds `ArrowRight` and `Home`.
    - Card element switch (D-4): `grep -q "card.href ? 'a' : 'article'" src/...CrashCourseRail.tsx` (or equivalent Tag-switch on `card.href`).
    - prev/next are buttons with aria-labels and disabled-at-ends: grep finds `aria-label="Previous"`, `aria-label="Next"`, `disabled={activeIndex === 0}`, and `disabled={activeIndex === content.cards.length - 1}`.
    - Heading is h2 via role/as: `grep -q 'role="display" as="h2"' src/...CrashCourseRail.tsx`; card title is `role="h3" as="span"` (D-6): `grep -q 'role="h3" as="span"' src/...CrashCourseRail.tsx`.
    - No weight-600 reintroduced: `grep -ci "font-weight" src/...CrashCourseRail.tsx` returns 0 (weights are token/role-driven, not inline).
    - `useReveal` wired on header only: `grep -q "useReveal()" src/...CrashCourseRail.tsx`.
  </acceptance_criteria>
  <done>The component compiles, lints clean, renders a header + rail + dot nav + prev/next, tracks active index from scrollLeft, drives aria-current/disabled state, scrolls via scrollToIndex (instant under reduced motion), supports Arrow/Home/End keyboard, and switches card element on href. Class names reference the (yet-to-be-written) CSS module by string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author content → render | `content` is author/CMS-supplied; `card.href` is interpolated into an anchor `href` and `card.title` into `aria-label`/visible text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `card.href` → `<a href>` | accept | Example/demo block; `content` is trusted author-authored fixture data, not end-user input. No runtime user input crosses this boundary. React escapes interpolated text by default. |
| T-03-02 | Information Disclosure | external Unsplash image URLs in story fixtures | accept | Public demo imagery only; flagged "Replace with licensed photography before production" in the fixtures file. No PII. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed in this phase (uses existing React + in-house primitives/hooks). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run typecheck` passes for the new files.
- `npm run lint` passes (ESLint + Stylelint) — proves no raw hex/px in the .tsx.
- The component is importable and renders the full interactive surface (verified end-to-end by plan 03-02's test).
</verification>

<success_criteria>
- `types.ts` exports `CrashCourseRailContent` + `CrashCourseRailCard` with the locked shape (optional `href`, required `{ src; alt }` image).
- `CrashCourseRail.tsx` exports `CrashCourseRail` + `CrashCourseRailProps`, composes Section→Container→Stack header + scroll-snap rail + dot nav + prev/next, holds component-local activeIndex + scrollToIndex + keyboard handlers, wired to `useReveal` on the header only.
- All 10 corrections/decisions reflected: D-2 (nav not tablist), D-3 (no deck-fan), D-4 (card href switch, no drawer), D-5 (component-local, no new hook), D-6 (h3 as span, 2 weights), reduced-motion guard (REQ-04/SC#2), Container default (1440 cap fine), Image fill deferred to CSS, chevrons bespoke SVG, eyebrow optional.
</success_criteria>

<output>
Create `.planning/phases/03-crash-course-block/03-01-SUMMARY.md` when done.
</output>
