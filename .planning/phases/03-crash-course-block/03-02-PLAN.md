---
phase: 03-crash-course-block
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/system/examples/CrashCourseRail/CrashCourseRail.module.css
  - src/system/examples/CrashCourseRail/CrashCourseRail.stories.tsx
  - src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx
  - src/system/examples/CrashCourseRail/index.ts
autonomous: true
requirements: [REQ-01, REQ-02, REQ-04, REQ-05, REQ-06]
must_haves:
  truths:
    - "The rail is a horizontal scroll-snap strip with a hidden scrollbar; cards snap to the start edge."
    - "Each card shows its image filling a rounded tile with a dark bottom scrim so the caption stays legible."
    - "The active dot widens to a pill; the rail/dots/cards/nav show a visible focus ring on keyboard focus."
    - "Under prefers-reduced-motion all transitions/transforms are off and content is fully visible + the rail still scrolls."
    - "A Storybook story renders the block with 8 cards and a second story with 3 cards proving dots scale and prev/next disable."
    - "Tests assert the accessibility contract (SC#2): labelled dot nav of buttons with aria-current, cards render with correct element, prev disabled at start, content visible without motion."
  artifacts:
    - path: "src/system/examples/CrashCourseRail/CrashCourseRail.module.css"
      provides: "scroll-snap rail, card fill+scrim, active-dot pill, round nav buttons, header rise, reduced-motion guard — token-only"
      contains: "scroll-snap-type: x mandatory"
    - path: "src/system/examples/CrashCourseRail/CrashCourseRail.stories.tsx"
      provides: "Default (8 cards) + FewCards (3 cards) stories, layout fullscreen, IMAGES fixtures"
      contains: "Examples/CrashCourseRail"
    - path: "src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx"
      provides: "MockObserver baseline + accessibility-contract assertions"
      contains: "aria-current"
    - path: "src/system/examples/CrashCourseRail/index.ts"
      provides: "public barrel"
      exports: ["CrashCourseRail", "CrashCourseRailProps", "CrashCourseRailContent"]
  key_links:
    - from: "src/system/examples/CrashCourseRail/CrashCourseRail.module.css"
      to: ".media img"
      via: "object-fit cover fill (Image ships height:auto)"
      pattern: "object-fit: cover"
    - from: "src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx"
      to: "CrashCourseRail component"
      via: "render + role/text queries"
      pattern: "render\\(<CrashCourseRail"
    - from: "src/system/examples/CrashCourseRail/index.ts"
      to: "./CrashCourseRail + ./types"
      via: "barrel re-export"
      pattern: "export"
---

<objective>
Style, story, test, and export the `CrashCourseRail` block authored in plan 03-01. Author the block-local CSS module (the NET-NEW scroll-snap rail CSS, card figure-fill + token scrim, active-dot pill, round nav buttons, header rise stagger, full `prefers-reduced-motion` guard — all token-only), the Storybook stories (Default 8 cards + FewCards 3 cards), the Vitest test covering the SC#2 accessibility contract, and the barrel `index.ts`.

Purpose: Deliver the visual faithfulness (REQ-05), the token-only constraint (REQ-02), the reduced-motion behavior (REQ-04), and the story + passing tests (REQ-06) — completing the full folder anatomy (REQ-01).
Output: `CrashCourseRail.module.css`, `CrashCourseRail.stories.tsx`, `CrashCourseRail.test.tsx`, `index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-crash-course-block/03-UI-SPEC.md
@.planning/phases/03-crash-course-block/03-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/system/examples/CrashCourseRail/CrashCourseRail.tsx
@src/system/examples/CrashCourseRail/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CrashCourseRail.module.css (scroll-snap rail, card fill+scrim, dots, nav, rise, reduced-motion)</name>
  <files>src/system/examples/CrashCourseRail/CrashCourseRail.module.css</files>
  <read_first>
    - src/system/examples/CrashCourseRail/CrashCourseRail.tsx (the exact class names this CSS must back: head, revealed, rise, rail, cell, card, media, cap, cardTitle, cardDesc, more, controls, dots, dot, dotActive, nav, navBtn)
    - src/system/examples/FooterCta/FooterCta.module.css (rise stagger lines 36-45; calc-radius idiom lines 55,78; token-only hover lift lines 97-101; reduced-motion guard lines 166-177; responsive `@media (width < 768px)`/`(width < 560px)` syntax; list reset lines 125-132)
    - src/system/examples/FeatureSpotlight/FeatureSpotlight.module.css (figure-fill `.frame img` lines ~25-31; rise gating; reduced-motion lines ~76-88)
    - src/system/examples/MTGNeighborhoods/NeighborhoodCard.module.css (image-fill + scrim line ~40 `color-mix(in oklch, var(--color-text-default) N%, transparent)`)
    - src/system/examples/MTGNeighborhoods/NeighborhoodPaginator.module.css (dot/active/focus-ring styling lines ~64-91, list reset)
    - .planning/phases/03-crash-course-block/03-UI-SPEC.md (Spacing lines 182-199, Color lines 224-251, Motion lines 256-268, Interaction sizing lines 274-282)
    - .planning/phases/03-crash-course-block/03-PATTERNS.md (the CSS sketches lines 212-291; "No Analog Found" scroll-snap guidance lines 411-419)
  </read_first>
  <action>
    Create `CrashCourseRail.module.css`, kebab-case classes matching the .tsx, token-only (NO raw hex, NO raw px — Stylelint + the project ban enforce this). Open with a short banner comment identifying it as block-local token-only styling.

    HEADER RISE (copy FooterCta lines 36-45 idiom): `.rise` transitions opacity + transform with `var(--motion-reveal-duration) var(--motion-reveal-easing) var(--rise-d, 0ms)`; `.head:not(.revealed) .rise { opacity: 0; transform: translateY(var(--space-3)); }`. Center the header column and constrain measure with `ch` units (heading `max-width: ~60ch; text-wrap: balance`; lead `max-width: 54ch; text-wrap: pretty`) — `ch` is not a px literal.

    SCROLL-SNAP RAIL (NET-NEW — author from SPEC §Motion line 260; confirmed safe, no ancestor sets overflow:hidden): `.rail { display: flex; gap: var(--space-stack-default); overflow-x: auto; scroll-snap-type: x mandatory; scrollbar-width: none; scroll-padding-inline: var(--space-container-pad-md); margin-top: var(--space-content-gap); margin-block: 0; padding: 0; list-style: none; }` plus `.rail::-webkit-scrollbar { display: none; }`. Reset the `<ul>` default list styling (no margin/padding/list-style, like NeighborhoodPaginator/FooterCta). Focus ring: `.rail:focus-visible { outline: var(--border-strong) solid var(--color-focus-ring); outline-offset: var(--space-1); }`. `.cell { flex: 0 0 clamp(258px, 23vw, 310px); scroll-snap-align: start; list-style: none; }` (the clamp min/max are layout dimensions, expressed via `clamp()` exactly as the SPEC sanctions; `vw` and `clamp` are allowed — the ban is on raw px literals, and these px appear inside `clamp()` per SPEC §Interaction line 275 which is the faithful DS expression of the source's `clamp(258–310px)`).

    CARD FILL + SCRIM (figure-fill copy FeatureSpotlight `.frame img`; scrim copy NeighborhoodCard line 40): `.card { position: relative; display: block; border-radius: calc(var(--radius-lg) * 1.5); overflow: clip; background: var(--color-surface-raised); text-decoration: none; height: clamp(366px, 31vw, 420px); }`. `.media { position: absolute; inset: 0; }` and the REQUIRED fill override `.media img { width: 100%; height: 100%; object-fit: cover; display: block; }` (Image ships `height:auto` — this is mandatory, the figure-wrapper precedent). Dark bottom scrim: `.card::after { content: ''; position: absolute; inset: 0; background: linear-gradient(to top, var(--color-overlay-scrim), transparent 55%); pointer-events: none; }` (or use `color-mix(in oklch, var(--color-text-default) N%, transparent)` per the coordChip precedent — either token form, NO raw rgba). `.cap { position: relative; z-index: 1; display: flex; flex-direction: column; gap: var(--space-stack-snug); color: var(--color-text-on-dark); }` positioned at the card bottom with inset via `--space-stack-loose`/`--space-stack-snug`. `.cardTitle`, `.cardDesc`, `.more` get `color: var(--color-text-on-dark)`; `.more { display: inline-flex; align-items: center; gap: var(--space-inline-default); }`. The card title/desc sizes come from the `Text` role — do NOT set `font-size` here. Do NOT set any `font-weight` (2-weight rule, D-6).

    CARD HOVER (restrained, `@media (hover: hover)` only, SPEC line 264): subtle shadow deepen + image `transform: scale(1.03)` (NOT 1.05) + reveal `.more`; transitions via `--motion-hover-duration`/`--motion-hover-easing`.

    ACTIVE DOT PILL (adapt NeighborhoodPaginator dot/active): `.dots { display: flex; align-items: center; gap: var(--space-inline-default); }` (nav reset: margin 0, padding 0). `.dot { width: var(--space-3); height: var(--space-3); border-radius: var(--radius-pill); background: transparent; border: var(--border-strong) solid var(--color-text-default); padding: 0; cursor: pointer; transition: width var(--motion-state-change-duration) var(--motion-state-change-easing); }`. `.dotActive { width: var(--space-7); background: var(--color-text-default); }` (the active pill, ~48px). `.dot:focus-visible { outline: var(--border-strong) solid var(--color-focus-ring); outline-offset: var(--space-1); }`.

    ROUND NAV BUTTONS + token lift (lift copy FooterCta lines 97-101): `.nav { display: flex; gap: var(--space-inline-default); }`. `.navBtn { display: inline-grid; place-items: center; width: var(--space-7); height: var(--space-7); border: 0; border-radius: var(--radius-full); background: var(--color-text-default); color: var(--color-surface-page); cursor: pointer; transition: transform var(--motion-hover-duration) var(--motion-hover-easing); }`. `.navBtn svg { width: var(--space-4); height: var(--space-4); }`. `.navBtn:disabled { opacity: 0.4; cursor: not-allowed; }`. `.navBtn:focus-visible { outline: var(--border-strong) solid var(--color-focus-ring); outline-offset: var(--space-1); }`. `@media (hover: hover) { .navBtn:not(:disabled):hover { transform: translateY(calc(var(--space-1) * -0.5)); } }` (token-only lift, SPEC line 265 mandates this over raw scale).

    CONTROLS ROW: `.controls { display: flex; justify-content: space-between; align-items: center; gap: var(--space-content-gap); margin-top: var(--space-content-gap); }`.

    RESPONSIVE (SPEC line 275, FooterCta media-query syntax): `@media (width < 768px)` step section rhythm down where needed; `@media (width < 560px) { .cell { flex-basis: 78vw; } .card { height: 104vw; } }`.

    REDUCED-MOTION GUARD (copy FooterCta lines 166-177): `@media (prefers-reduced-motion: reduce) { .rise { transition: none !important; opacity: 1 !important; transform: none !important; } .card, .media img, .dot, .navBtn { transition: none !important; transform: none !important; } }`. (The `scrollToIndex` JS-side instant jump is handled in the component, plan 03-01.)
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` passes (Stylelint clean — proves NO raw hex; the px-in-clamp/ch/vw layout values are allowed).
    - Scroll-snap rail present (NET-NEW): `grep -q "scroll-snap-type: x mandatory" src/system/examples/CrashCourseRail/CrashCourseRail.module.css` AND `grep -q "scroll-snap-align: start" ...module.css` AND `grep -q "scrollbar-width: none" ...module.css` AND `grep -q "::-webkit-scrollbar" ...module.css`.
    - Image fill override present (mandatory): `grep -q "object-fit: cover" ...module.css` under a `.media img` rule.
    - Token scrim, no raw rgba/hex: `grep -Eq "color-mix\(in oklch|--color-overlay-scrim" ...module.css` AND `grep -ci "rgba(" ...module.css` returns 0 AND `grep -ciE "#[0-9a-f]{3,8}" ...module.css` returns 0 (no raw hex).
    - Active-dot pill: `grep -q ".dotActive" ...module.css` AND `grep -q "var(--space-7)" ...module.css`.
    - Token-only nav lift (no raw scale magic number on nav): `grep -q "translateY(calc(var(--space-1) \* -0.5))" ...module.css`.
    - Focus ring uses the token: `grep -c "var(--color-focus-ring)" ...module.css` is >= 3 (rail, dots, nav).
    - Reduced-motion guard present: `grep -q "prefers-reduced-motion: reduce" ...module.css` AND within that block grep finds `transition: none !important` and `transform: none !important`.
    - No font-weight hardcoded (D-6, 2-weight rule): `grep -ci "font-weight" ...module.css` returns 0.
    - Card radius via calc idiom: `grep -q "calc(var(--radius-lg)" ...module.css`.
  </acceptance_criteria>
  <done>The CSS module compiles, Stylelint passes, backs every class name the component references, implements the scroll-snap rail, card fill + token scrim, active-dot pill, round nav buttons with token lift, focus rings, and a full reduced-motion guard — token-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author stories + test + barrel (accessibility contract, SC#2)</name>
  <files>src/system/examples/CrashCourseRail/CrashCourseRail.stories.tsx, src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx, src/system/examples/CrashCourseRail/index.ts</files>
  <read_first>
    - src/system/examples/FooterCta/FooterCta.stories.tsx (meta/story shape: `title: 'Examples/...'`, `parameters: { layout: 'fullscreen' }`, `type S = StoryObj<...>`, IMAGES import path `../../../stories/_fixtures/images`)
    - src/system/examples/FooterCta/FooterCta.test.tsx (MockObserver stub + beforeEach lines 9-18 — copy VERBATIM; role/text query patterns lines 50-97; the no-motion baseline test lines 93-97; the jsdom-limitation comment lines 6-8,94)
    - src/system/examples/FooterCta/index.ts (barrel shape, lines 1-2)
    - src/stories/_fixtures/images.ts (available keys: water, portrait, archipelago, city, industry, mtgHeroCoat, mtgHeroDadKid, mtgHeroCouch)
    - src/system/examples/CrashCourseRail/CrashCourseRail.tsx (the rendered DOM the test asserts against — h2 heading, dot nav, card elements, prev/next)
    - src/system/examples/CrashCourseRail/types.ts (CrashCourseRailContent shape for the fixture)
    - .planning/phases/03-crash-course-block/03-UI-SPEC.md (Copywriting Contract lines 286-305 — the 8 card copy; SPEC line 68 story requirements; line 69 test requirements)
    - .planning/phases/03-crash-course-block/03-PATTERNS.md (story + test pattern assignments lines 295-342)
  </read_first>
  <behavior>
    Test assertions (the SC#2 accessibility contract — write these as the spec, then make them pass):
    - Renders the heading as an `<h2>` (`getByRole('heading', { level: 2 })` has the heading text).
    - Renders the eyebrow (if present) and lead text.
    - Renders ALL 8 card titles (`getByText` per title, or `getAllByText`/loop).
    - A card WITH `href` is a link with that href (`getByRole('link', { name: /fika/i })` → `toHaveAttribute('href', ...)`).
    - A card WITHOUT `href` is NOT a link (`queryByRole('link', { name: /<that title>/i })` returns null) — use a fixture variant where one card omits href.
    - The dot nav is a labelled navigation of buttons (`getByRole('navigation', { name: /pagination/i })`; `getAllByRole('button', { name: /go to/i })` length === card count).
    - The first dot has `aria-current="true"` at mount; the rest do not.
    - The Previous button is `disabled` at start (`getByRole('button', { name: /previous/i })` → `toBeDisabled()`); Next is enabled.
    - Content is visible without motion (MockObserver baseline — no IntersectionObserver firing, hidden pre-state is CSS-only).
    - jsdom limitation: real scroll/`scrollTo` is unavailable — assert handler WIRING (button presence/labels + initial aria-current), NOT real scroll position; add a one-line comment noting this (mirroring FooterCta/ThemedAccordion convention).
  </behavior>
  <action>
    STORIES — copy the meta/story shape from FooterCta.stories.tsx. Import `type { Meta, StoryObj }` from `@storybook/react`, `{ CrashCourseRail }` from `./CrashCourseRail`, `type { CrashCourseRailContent }` from `./types`, and `{ IMAGES }` from `../../../stories/_fixtures/images` (verbatim relative path). Build a `content: CrashCourseRailContent` const with the 8 cards from the SPEC Copywriting Contract (Fika, Archipelago days, Hagabullen, Midsummer, First outdoor coffee of spring, Rain with character, Långgatorna, Cold swims) using their exact copy, each mapped to an IMAGES fixture (reuse keys — e.g. archipelago→Archipelago days, water→Cold swims, city→Långgatorna), each card given a demo `href` (e.g. `'#fika'`). Set the optional eyebrow `'№ — A crash course'`, the heading `'A crash course in Gothenburg life'`, and the lead from the SPEC. `meta = { title: 'Examples/CrashCourseRail', component: CrashCourseRail, parameters: { layout: 'fullscreen' } }`; `export default meta`; `type S = StoryObj<typeof CrashCourseRail>`. Export `Default: S = { args: { content } }` and `FewCards: S = { args: { content: { ...content, cards: content.cards.slice(0, 3) } } }` (proves dots scale + prev/next disable, SPEC line 68).

    TEST — create `CrashCourseRail.test.tsx`. Copy the `MockObserver` stub + `beforeEach` VERBATIM from FooterCta.test.tsx lines 9-18 (jsdom has no IntersectionObserver; the no-motion baseline holds). Add a local `content: CrashCourseRailContent` fixture (≥3 cards is enough; include the 8 titles if convenient) where AT LEAST ONE card has a `href` and AT LEAST ONE omits `href` (to assert the link/non-link D-4 contract). Write one `describe('CrashCourseRail')` suite with `it(...)` cases covering every assertion in <behavior> using accessible queries (`getByRole`, `getByText`, `queryByRole`, `getAllByRole`). Add the one-line jsdom-limitation comment explaining that real scroll position cannot be asserted (mirror FooterCta lines 6-8,94 + CONVENTIONS.md line 89). Match codebase test style (single quotes, no trailing semicolons).

    BARREL — create `index.ts` copying the FooterCta/index.ts shape, renamed: `export { CrashCourseRail, type CrashCourseRailProps } from './CrashCourseRail'` and `export type { CrashCourseRailContent } from './types'`.
  </action>
  <verify>
    <automated>npx vitest run src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx` passes (all cases green).
    - `npm run typecheck` and `npm run lint` pass.
    - Test asserts the labelled dot nav of buttons with aria-current: `grep -q "navigation" src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx` AND `grep -q "aria-current" ...test.tsx` AND `grep -q "Go to" ...test.tsx`.
    - Test asserts cards render and the link/non-link D-4 contract: `grep -q "getByRole('link'" ...test.tsx` AND `grep -q "queryByRole('link'" ...test.tsx`.
    - Test asserts prev disabled at start: `grep -qi "toBeDisabled" ...test.tsx` AND `grep -qi "previous" ...test.tsx`.
    - Test asserts content-without-motion baseline: `grep -q "MockObserver" ...test.tsx` AND the jsdom-limitation comment is present (`grep -qi "jsdom" ...test.tsx`).
    - Test asserts the h2 heading: `grep -q "level: 2" ...test.tsx`.
    - Stories: `grep -q "title: 'Examples/CrashCourseRail'" src/system/examples/CrashCourseRail/CrashCourseRail.stories.tsx` AND `grep -q "layout: 'fullscreen'" ...stories.tsx` AND both `export const Default` and `export const FewCards` present AND `grep -q "_fixtures/images" ...stories.tsx`.
    - Barrel exports all three public symbols: `grep -q "export { CrashCourseRail" src/system/examples/CrashCourseRail/index.ts` AND `grep -q "CrashCourseRailContent" ...index.ts`.
  </acceptance_criteria>
  <done>The test suite passes and asserts the full SC#2 accessibility contract (labelled dot nav of aria-current buttons, link vs non-link cards, prev disabled at start, content visible without motion, h2 heading). Two stories render. The barrel exposes the public API. typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author content → render | Story/test fixtures supply `content`; `card.href` flows to an anchor, `card.title` to text + aria-label. Trusted author data, not end-user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | external Unsplash image URLs in story fixtures | accept | Public demo imagery; `_fixtures/images.ts` is flagged "Replace with licensed photography before production". No PII. |
| T-03-04 | Spoofing | demo `href` values (e.g. `#fika`) | accept | Demo-only anchors in an example block explicitly banner-commented "Replace before production with a CMS-wired block." |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (uses existing Storybook 8.6 + Vitest 4 + Testing Library already in package.json). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/system/examples/CrashCourseRail/CrashCourseRail.test.tsx` passes (the SC#2 accessibility contract is green).
- `npm run typecheck` and `npm run lint` pass for the full folder (CSS Stylelint + TS/TSX ESLint, no raw hex/px).
- `npm test` (full suite) stays green — the new block does not regress existing tests.
- Storybook renders Default (8 cards) + FewCards (3 cards); reduced-motion handled by the global toolbar decorator.
</verification>

<success_criteria>
- Full folder anatomy complete (REQ-01): `CrashCourseRail.tsx`, `types.ts`, `CrashCourseRail.module.css`, `CrashCourseRail.stories.tsx`, `CrashCourseRail.test.tsx`, `index.ts`.
- Token-only CSS (REQ-02): no raw hex, scrim/colors/spacing/motion all via tokens; Stylelint passes.
- Reduced-motion behavior (REQ-04): CSS guard kills all transitions/transforms and keeps content visible + rail usable.
- Visually faithful to variant C (REQ-05): scroll-snap rail, rounded image cards with dark scrim caption, active-dot pill, round prev/next.
- Story + passing tests (REQ-06): two stories, full accessibility-contract test green.
</success_criteria>

<output>
Create `.planning/phases/03-crash-course-block/03-02-SUMMARY.md` when done.
</output>
